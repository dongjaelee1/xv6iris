You are the agent for lane EXEC-SEAM of the xv6iris project (Rocq 9.0.1 / Iris weakest-precondition proofs of a RISC-V xv6 kernel with an "echo hello world" application theorem on top). YOUR CHECKOUT IS /shared/xv6iris-2-disc (absolute paths everywhere; never touch /shared/xv6iris-2, -sup, -tlw, -notes; never push; never edit claude-notes/; iris/_CoqProject gets a bare file row only if you add a file). You are the only lane editing code; one READ-ONLY documentation lane runs in -tlw.

SETUP: `cd /shared/xv6iris-2-disc && git fetch origin && git checkout -B lane/exec-seam origin/main` (origin/main = 146e7cac1 code + notes commits; the checkout is clean on lane/residuals which is merged). Confirm `git log --oneline -3`. Your VM tree is /mnt/rocq/trees/_shared_xv6iris-2-disc.

READ FIRST, IN FULL: claude-notes/README.md; claude-notes/durable-notes.md (whole); claude-notes/projects/checkpoint-2026-09-14-step3.md; claude-notes/projects/checkpoint-2026-09-16.md §6 (build/gate/commit rules); claude-notes/projects/app-echo.md notes dated 2026-09-14 "RESIDUALS (A) LANDED; (B) BLOCKED AT THE EXEC SEAM", "SH-LINE R3 RE-SURVEYED", "IO-LEAF STEP 4 LANDED", "M6b LANDED"; claude-notes/projects/handoff-2026-09-16/residuals-report.md (WHOLE -- its "(B) exactly what is missing" paragraph is your edit list; its (C)/(D) chain is your second and third milestone), brief-residuals.md ((C) and (D) verbatim -- you do them after (B)), step4-sh-report.md §4/§5, m6b-init-report.md ("THE REFUND AT THE LEND'S SHAPE -- NEW file UkRunExecRef.v": the `_refR` precedent you copy), r3-survey-2.md §2 (why `ush_wcp`'s affine arm blocks R3). THE CODE, the seam and its relay (read in this order): iris/SpecKexec.v (`exec_slot_pre` ~799-826, the two wands over `W'`); iris/ProofKexec.v (`exec_key U' sts gn cs pidv na` ~731/782: the key the kernel builds -- its `uvis_ch`/`uvis_pid` are `cs`/`pidv` by reflexivity) and every other discharger of `exec_slot_pre` (grep; the residuals report names ProofKexec A/D and ProofSysExec); iris/SpecSysExec.v (`sys_exec_slot_pre`, `sys_exec_au_pre`), `exec_au_pre` (grep), iris/SpecSyscall.v and iris/SpecUsertrap.v (where the exec bundle's slot is named), iris/UexecSG.v (`sbundle_at … USYS_exec`), iris/UexecApply.v (~1059), iris/UexecRet.v (the exec arm), iris/UexecExecInst.v (`sbundle_exec_intro`), iris/PinnedExec.v (`pinned_exec_bundle`, `pinned_exec_bundle_at`, `pex_slot`); iris/UkRun.v (`udepw_at_ref`, `urun_ids`, `urun`'s fields: `uheap`, `ufd_auth`, `ucwd_auth`, the children and pid authorities) and iris/UkRunExecRef.v (`udepw_at_refR`, `sbundle_pay_refR`, `wp_uk_ecall_exec_at_cwd_refR`); iris/UkInit.v (`init_exec_sup_pos`, `init_lend_ref`, `wp_kinit_exec`), iris/UkInitMain.v (`wp_kinit_fork`'s child arm ~1005: `uch (ukn_ch N') ∅` and `∃ p, ⌜p <> 1⌝ ∗ upid …` are DROPPED with `_`; `wp_kinit_main_child`), iris/UInitSh.v (`init_exec_sup_of_sh_slot`, its refund arm, the `init_lend_ref` intro twin), iris/UShKernel.v (`sh_uexec_slot`, `sh_slot_of_kexec`, `wp_ksh_start`: what facts about `W` the entry gets; `uslot_of_urun_all` and its `uch_any`/`upid_any`), iris/UkSh.v (`ush_pstate`: `uch_any γch ∗ upid_any γpid`, `ush_wcp`, `ush_posb_at`, `ush_gets_done_0/_line`, `ksh_w_of_wcp`, `sh_deps`), iris/UkShFork.v (the wait re-entry's not-my-child arm ~826-830, the fork's affine arm ~852, `wp_kshr_wait_pid`'s use), iris/UexecRet.v (`uwait_ans_pid_mine`), iris/UserChildren.v (`uch`, `uch_any`, `upid`, `upid_any`, `upid_agree`), iris/UInitBoot.v (`echo_Hinit_boot`, `init_deps_of_sup`, `Htsw`/`Hstw`), iris/UInitBootAdequacy.v (`Hsh_owed`).

THE TASK, three milestones, each a separate green commit:
(B) THE EXEC SEAM CARRIES THE CHILDREN SET AND THE PID. Plain terms: the exec seam is the kernel exec contract's program-facing wand (`SpecKexec.exec_slot_pre`): it hands the exec'd program its own weakest precondition at the new key `W'` (`uvis`, the process's visible state record), pinning the image, the working directory, the lazy flag and the payload -- but not `uvis_ch W'` (the children set) or `uvis_pid W'` (the pid), though both are the CALLER's `cs`/`pidv` by reflexivity where the kernel discharges the wand. Add two parameters `cs pidv` and the pure rows `⌜uvis_ch W' = cs⌝ ∗ ⌜uvis_pid W' = pidv⌝` to both wands of `exec_slot_pre`, discharge them by reflexivity at every kernel discharge site, relay them through the spec/proof chain the residuals report lists (SpecSysExec, exec_au_pre, SpecSyscall, SpecUsertrap, UexecSG's `sbundle_at … USYS_exec`, UexecApply, UexecRet's exec arm, UexecExecInst.sbundle_exec_intro, PinnedExec.pex_slot / pinned_exec_bundle(_at)) as pass-throughs, then give the U-tier supplier what pins them: a `_refR`-style twin of `UkRun.udepw_at_ref`/`UkRunExecRef.udepw_at_refR` that hands the supplier the record's children/pid authorities (`urun_ids`) beside `uheap`/`ufd_auth` (copy M6b's precedent: a new leaf file or an extension of UkRunExecRef.v, NOT an edit of UexecSG/UkRun/UkRunSys), so init's fork child's `uch (ukn_ch N') ∅` and `⌜p <> 1⌝ ∗ upid (ukn_pid N') p` (UkInitMain ~1005, dropped today) pin the shell's key: `UkInit.init_exec_sup_pos`/`init_lend_ref` -> `UInitSh.init_exec_sup_of_sh_slot` -> `UShKernel.sh_uexec_slot`/`sh_slot_of_kexec` -> `UkSh.ush_pstate` carries `uch γch ∅`-then-`uch γch Sc` and `upid γpid p ∗ ⌜p <> 1⌝` instead of `uch_any`/`upid_any`; the wait re-entry's not-my-child arm in `UkShFork` then closes by `uwait_ans_pid_mine` (the reaped child is the forked one) and DISAPPEARS. KERNEL CONTRACT TEXT: `SpecKexec`/`SpecSysExec`/`SpecSyscall`/`SpecUsertrap` WILL change -- these add pure rows that are true by reflexivity at the discharge (the program-facing wand gets STRONGER, the kernel proof owes MORE and pays it), never a new assumption and never a weakening; report every changed contract statement OLD/NEW verbatim and confirm the audit md5 is unchanged. Cone ~150 files: batch the edits and build once per milestone; use rocq-warm per file while editing (rebuild edited dependencies' .vo first).
(C) THE KILLS, verbatim from brief-residuals.md (C): `UkInit.init_rd_cred Wb n := Wb n`; `init_lend_cred`'s third arm -> `T`; `UkSh.ush_wcp`'s third arm, `ush_wcp_triv`, `ush_gets_done_0`/`ush_gets_done_line`'s affine arms, `ush_at_of_pm`/`UShLine.ush_at_of_mid` (the right-arm assemblers), `UInitSh`'s mapping of the affine arm -- all deleted; `grep -n "∨ True" iris/Uk*.v iris/USh*.v iris/UInit*.v iris/UEcho*.v` empty of non-comment hits (list any survivor with its reason).
(D) THE FREE LAW, verbatim from brief-residuals.md (D): `sh_deps` on the paid walk becomes `□ (T -∗ UkSh.sh_deps)` (only taint arms spend it: getcmd's, the fork's taint arm), derived at the top from the application supply as `init_deps_of_sup` does; the generic runner/diagnostic lemmas keep their unconditional premise; `sh_deps` removed where only forwarded; `Hsh_owed`'s FIRST conjunct deleted in iris/UInitBootAdequacy.v (report OLD/NEW; the audit md5 changes if the hypothesis count changes -- print the audit and explain every line). Note: lane R3 will later delete the SECOND conjunct too (r3-survey-2.md) -- do not do R3's work; keep `sh_pay_rest` as is.

RULES: everything fully proved; no Admitted; no new `∨ True`; pure facts by `vm_compute`/`reflexivity` in small closed lemmas, never on goals with variables; every file must `Require` what it names; name Timeless/Persistent instance binders explicitly; do not rename what you need not; grep the whole tree for users before deleting; no prose in iris/_CoqProject; keep every kernel Proof file's change to the reflexivity discharge and pass-throughs.

BUILD PROCEDURE (never deviate): edit loop with rocq-warm (claude-notes/rocq-warm.md): `./gcp-rocq/run-on-gcp -q bash -lc 'cd /mnt/rocq/trees/_shared_xv6iris-2-disc/iris && /mnt/rocq/rocq-warm/rocq-warm check SpecKexec.v'` (the sync deletes the .vo of every file you edited; rebuild edited dependencies first with `make -f CoqMakefile -j8 A.vo` in the VM tree; a stale intermediate gives "makes inconsistent assumptions"). Full builds: from /shared/xv6iris-2-disc, `./gcp-rocq/vmbuild.sh xv6iris-2-disc es-1` (then es-2, ...), DETACHED (`setsid nohup ./gcp-rocq/vmbuild.sh xv6iris-2-disc es-1 > /tmp/claude-0/-shared-xv6iris-2/2c386d62-e2e4-4d9f-b3c5-841bab14908e/scratchpad/es-1.out 2>&1 &`), then POLL the .out file for its `EXIT=` line with short sleeps FROM YOUR OWN TURN -- a detached build does not wake you; NEVER end your turn while a build is running (the previous lane did, and lost an hour); errors via `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/es-1.log'`. One build at a time in your tree; never start one before the previous log has its EXIT= line; never pattern-kill on the VM; a run-on-gcp sync IS a build.

GATE before you report (per commit): EXIT=0 and zero ^Error; VM `make -f CoqMakefile -n` prints no `ROCQ compile`; `cd /mnt/rocq/trees/_shared_xv6iris-2-disc && make audit-only 2>&1 | grep -v '^make\|^cd ' | md5sum` (57f7327206c4b276d05035342fea8ecf until (D); after (D) print the audit and explain); `python3 tools/lemma_diff.py --ref origin/main` with every GONE/CHANGED/NEWAXIOM justified; no Admitted; `./gcp-rocq/run-on-gcp --check-dumps` clean. Commit by explicit path on lane/exec-seam (author = the checkout's config; never `-c user.email`; end the message with `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01VzSVeaFhoeWNGQosZ6V4gt`). Never `git stash`, `git add -A`, `git reset`, `git commit -a`, `--amend`.

REPORT (final answer; also to /tmp/claude-0/-shared-xv6iris-2/2c386d62-e2e4-4d9f-b3c5-841bab14908e/scratchpad/exec-seam-report.md): commit hashes per milestone; every changed KERNEL CONTRACT statement OLD/NEW verbatim; the new twin's statement; `ush_pstate`, `sh_uexec_slot`'s premise list, `init_rd_cred`/`init_lend_cred`/`ush_wcp` NEW, `Hsh_owed` OLD/NEW; every deleted name and where its users went; every `∨ True` left anywhere in iris/Uk*.v iris/USh*.v iris/UInit*.v iris/UEcho*.v with its reason; every remaining spender of `sh_deps`; the gate lines per commit including the audit text after (D); anything the brief did not settle. Plain concurrent-separation-logic and xv6 terms, every term of art defined on first use. If you run out of context, stop at a green committed state and write where a successor picks up.

You are the agent for lane M6B-INIT of the xv6iris project (Rocq 9.0.1 / Iris weakest-precondition proofs of a RISC-V xv6 kernel with an "echo hello world" application theorem on top). YOUR CHECKOUT IS /shared/xv6iris-2-sup (absolute paths everywhere; never touch /shared/xv6iris-2, -disc, -tlw, -notes; never push; never edit claude-notes/; iris/_CoqProject gets a bare file row only if you add a file). A sibling lane STEP4-SH works concurrently in -disc on the SHELL side (UkSh.v, UkSh*.v, USh*.v, UEcho*.v, EchoLinks*.v, UShLine.v and the shell half of UInitSh.v/UInitBoot.v). DO NOT edit any of those shell files. Your files: UkInit.v, UkInitMain.v, UkInitLit.v/UkInitPrintf.v if needed, UInitDiag.v, UInitBanner.v, UInitKernel.v, UInitFd.v, UserConsole.v, and in UInitSh.v / UInitBoot.v ONLY what init's new shapes force (the coordinator merges the two lanes' edits to those two files by hand -- keep yours localized and LIST every hunk in the report).

SETUP: `cd /shared/xv6iris-2-sup && git fetch origin && git checkout -B lane/m6b-init origin/main` (origin/main = b65275c81 or later; the checkout is clean on lane/init-diag which is merged). Confirm `git log --oneline -3`. Your VM tree is /mnt/rocq/trees/_shared_xv6iris-2-sup.

READ FIRST, IN FULL: claude-notes/README.md; claude-notes/durable-notes.md (whole file); claude-notes/projects/checkpoint-2026-09-14-step3.md (§1 rulings, §3b, §4); claude-notes/projects/checkpoint-2026-09-16.md §6 (build/gate/commit rules); claude-notes/projects/app-echo.md notes dated 2026-09-14: "INIT-DIAG LANDED", "DIE-DW CORRECTED" (the recipe you implement), "IO-LEAF STEP 3 LANDED", "PROLOGUE-ALTS-3 LANDED", and the older "IO-LEAF M6a(2) LANDED"; claude-notes/projects/handoff-2026-09-16/init-diag-report.md (whole -- §1 "THE ROUND-OPEN CREDENTIAL, EXACTLY", §3 the two laws, "HOW EACH DIE DIAGNOSTIC IS WRITTEN TODAY, AND WHAT IT WOULD NEED"); handoff-2026-09-16/step3-interface.md and the "init side" paragraph of the step-3 landed note; handoff-2026-09-16/prologue-alts-3-report-3.md ("State for the coordinator"). THE CODE: iris/UkInit.v (`init_rd_cred`, `init_rd`, `init_pay_of_lend`, `init_lend_cred`, `init_exec_sup_pos`, `init_exec_sup_lend`, `init_cons_sup`, `kinit_banner_pay`, `wp_kinit_wait` and the `_null` leaf it calls, `wp_kinit_exec`, `kinit_w1`, `kinit_w1_of_law`); iris/UkInitMain.v (whole: the three `wp_kinit_main_die_*` at 167/279/391, `wp_kinit_main_child`, `wp_kinit_fork` -- its -1 arm at ~937 discards the refunded `Rc`, `kinit_banner0`, `kinit_ban_law`, `kinit_lent`, `wp_kinit_banner`, `wp_kinit_main_loop` -- the "wait itself failed" arm ~1700 and the redeem ~1610, `wp_kinit_main_from_1e`, `_repair_tail`, `_repair`, `wp_kinit_main`, `wp_kinit_start`); iris/UkInitPrintf.v (`wp_kinit_printf`, `wp_kinit_printf_chain`); iris/UInitDiag.v and iris/EchoLinksPro.v (whole -- INIT-DIAG's deliverables: `kinit_pro`, `kinit_own_of_pro`, `kinit_banner_law_pro_holds`, `kinit_execfail_law_holds`, `kinit_forkfail_law_holds`, `kinit_w1_of_link_pdiag`, `ewc_pro`, `ewc_pdiag`, `ewc_pdiag_done_1`, `ewc_ban_done_pro`); iris/UInitBanner.v (`kinit_ban`, `kinit_own`, `kinit_ban0_of_eturn`, `kinit_banner_law_holds`, `kinit_ban_law_holds`, `kinit_own_is_cred`); iris/UInitKernel.v (`init_uexec_slot`, `init_slot_of_kexec`, `init_boot_pay` -- TRUSTED, `init_boot_con`); iris/UInitFd.v (`ufd_head`, `ufd_row`, `ufd_head_open_row`, `ufd_head_of_row`); iris/UserConsole.v (`ucons_pay`, `uinit_tok`, `uinit_lend_c`, `uinit_redeem`, `ucons_pay_mono`); iris/UkRunSys.v (`wp_uk_ecall_wait_null_pid` ~2321 -- the `_pid` twin whose -1 row is `Sc' = ∅`), iris/UexecRet.v (`uwait_ans`, `uwait_ans_pid`, `uwait_ans_pid_mine`), iris/UserChildren.v (`upid`, `uch`), iris/ChildTok.v (`child_tok`, `gen_pay`, `gen_uniq_tok`, `exit_tok_tok_ne`); iris/UInitSh.v (`init_exec_sup_of_sh_slot` -- how it reads `init_lend_cred` off the lent ledger and lands it in the shell's `ush_wcp`), iris/UInitBoot.v (`init_cons_sup_of_sh_slot`, `echo_Hinit_boot`: the `Wc`/`Wb`/`Rdl` instantiations, the boot payment from `kinit_ban0_of_eturn`, the law from `kinit_ban_law_holds`).

THE TASK: M6b = init's diagnostics through the links, and the deletion of `die_dw`. Plain terms: /init's restart loop prints its banner, forks a shell, waits, and restarts; three diagnostics can be printed -- "init: exec sh failed" (the forked child's exec failed, `wp_kinit_main_die_de`), "init: fork failed" (`_die_df`), and "init: wait returned an error" (`_die_dw`). All three go through the FREE write law today (`udepw_law 16`, the flagged deposit = a placeholder that lets a console write count as paid without the era's credential). After M6b: (1) `die_dw` is DELETED: a killed process never returns to user mode, so wait's -1 at user level means "my own child set is empty"; `UkInit.wp_kinit_wait` takes init's pid row `UserChildren.upid (ukn_pid N) 1` (init's pid is the literal 1 -- find where init's run carries it; TRAP-ROWS-4 B1b) and calls `UkRunSys.wp_uk_ecall_wait_null_pid`; in `wp_kinit_main_loop`'s "wait itself failed" arm derive `cs = ∅` from the row and the answer, contradict `γsh ∈ cs`; delete `wp_kinit_main_die_dw` and the `0x9c8` literal's 29-byte family. (2) `die_de` and `die_df` are PAID through INIT-DIAG's laws: the credential init lends at the fork is the ROUND-OPEN shape `kinit_pro n` (the `wr_pro` shape as a credential; INIT-DIAG's finding: `wr_owed`'s two arms are not separable by anything init holds, so init must keep `kinit_pro` across its fork rather than `kinit_own`), so the lend family's console arm becomes a new opaque family `Wp n` with the law `Wp n -∗ Wc n 0` used at the shell's entry (`UInitSh.init_exec_sup_of_sh_slot` converts it to the shell's `ush_wcp` both-console arm; top: `Wp := UInitDiag.kinit_pro`, the law `kinit_own_of_pro` + `kinit_own_is_cred`), init's banner law leaves `Wp n` (`kinit_banner_law_pro_holds`); `wp_kinit_fork`'s -1 arm KEEPS the refunded `Rc` and "init: fork failed" is written through `wp_kinit_printf_chain` with `kinit_forkfail_law_holds`'s `∃ Ch` (18 bytes, leaving `emp` -- say what init's round does with its lease afterwards: init exits at `die_df`, so the lease is dropped there, and say whether that is admitted: init's exit payload is `fun _ => True`); the exec-failed child's "init: exec sh failed" through `kinit_execfail_law_holds` (21 bytes, leaving `kinit_ban n` = `Wb n`), so the child's own `exit(1)` pays the pair `UkInit.init_rd Rdl Wb n` with a REAL `Wb n` (today `init_pay_of_lend` takes the affine arm) -- the refund `wp_kinit_exec` hands back is `ukn_pay N' (-1)` built by the shell-slot's refund arm from the lend; check what `init_exec_sup_of_sh_slot`'s refund arm produces and restate `wp_kinit_exec`'s refund at the LEND shape (`upos ∗ ucons_pay Rdl (-1) ∗ init_lend_cred …`) if the credential has to survive to the diagnostic. (3) The affine arms: `UkInit.init_rd_cred Wb n := Wb n ∨ True` and `init_lend_cred`'s third `True` arm exist because the SHELL's exits do not all hand back `Wb n` yet (lane STEP4-SH is making them); on the taint the honest arm is `T`, not `True`. Re-cut `init_lend_cred`'s third arm to `T` if the walk supports it (the banner's taint-row case and the token's taint arm have `T` in hand; the token's affine credential arm does not -- keep that ONE `∨ True` named, `init_rd_cred`, and say precisely what the shell must guarantee for it to become `Wb n`).

RULES: everything fully proved; no Admitted; no new `∨ True`; pure facts about literals by `vm_compute`/`reflexivity` in small closed lemmas, never on goals with variables; every file must `Require` what it names; name Timeless/Persistent instance binders explicitly (`HRdl`/`HWb` style -- an anonymous second one is `H0` and clashes with the proofs' asserts); do not rename what you need not; grep the whole tree for a lemma's users before deleting it; no prose in iris/_CoqProject.

BUILD PROCEDURE (never deviate): edit loop with rocq-warm (claude-notes/rocq-warm.md): `./gcp-rocq/run-on-gcp -q bash -lc 'cd /mnt/rocq/trees/_shared_xv6iris-2-sup/iris && /mnt/rocq/rocq-warm/rocq-warm check UkInitMain.v'` (the sync deletes the .vo of every file you edited, so once you touch A.v a warm check of B.v that imports A fails with "Cannot find a physical path bound to logical path A"; rebuild A first: `./gcp-rocq/run-on-gcp -q bash -lc 'cd /mnt/rocq/trees/_shared_xv6iris-2-sup/iris && make -f CoqMakefile -j8 A.vo'`; a stale intermediate gives "makes inconsistent assumptions" -- make its .vo too). Full builds: from /shared/xv6iris-2-sup, `./gcp-rocq/vmbuild.sh xv6iris-2-sup m6b-1` (then m6b-2, ...), DETACHED (`setsid nohup ./gcp-rocq/vmbuild.sh xv6iris-2-sup m6b-1 > /tmp/claude-0/-shared-xv6iris-2/2c386d62-e2e4-4d9f-b3c5-841bab14908e/scratchpad/m6b-1.out 2>&1 &`), then POLL the .out file for its `EXIT=` line with short sleeps from your own turn -- a detached build does not wake you when it finishes, so never end your turn with a build running unless you have written the report; errors via `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/m6b-1.log'`. One build at a time in your tree; never start one before the previous log has its EXIT= line; never pattern-kill on the VM (other agents build there); a run-on-gcp sync IS a build.

GATE before you report: EXIT=0 and zero ^Error; VM `make -f CoqMakefile -n` prints no `ROCQ compile`; `cd /mnt/rocq/trees/_shared_xv6iris-2-sup && make audit-only 2>&1 | grep -v '^make\|^cd ' | md5sum` = 57f7327206c4b276d05035342fea8ecf (the thirteen; if different, print the audit and explain every line); `python3 tools/lemma_diff.py --ref origin/main` with every GONE/CHANGED justified (`wp_kinit_main_die_dw` and the 0x9c8 family are expected GONE); no Admitted; `./gcp-rocq/run-on-gcp --check-dumps` clean. TRUSTED STATEMENTS: `UInitKernel.init_boot_pay` (it will gain `Wp` if the banner law's result family changes) and `Hsh_owed`/`sh_pay_rest`/EchoDisc -- report OLD/NEW verbatim for any you change. Commit by explicit path on lane/m6b-init (author = the checkout's config; never `-c user.email`; end the message with `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01VzSVeaFhoeWNGQosZ6V4gt`). Never `git stash`, `git add -A`, `git reset`, `git commit -a`, `--amend`. Land green milestones as separate commits if natural: (A) `die_dw` deleted on the `_pid` row; (B) the two diagnostics through the links + the `Wp` lend.

REPORT (final answer; also to /tmp/claude-0/-shared-xv6iris-2/2c386d62-e2e4-4d9f-b3c5-841bab14908e/scratchpad/m6b-init-report.md): the commit hashes; every new/changed definition and lemma statement VERBATIM (`init_lend_cred`, `init_exec_sup_pos`, `kinit_ban_law`, `wp_kinit_wait`, `wp_kinit_fork`'s -1 arm, the two die lemmas, `init_boot_pay` OLD/NEW); every deleted name and where its users went; EVERY `∨ True` left in init's files, by name, with the obligation that kills it; the exact hunks you made in UInitSh.v and UInitBoot.v; which spenders of `udepw_law 16` (init's free write law) remain, if any; the gate lines; anything you had to decide that this brief did not settle. Plain concurrent-separation-logic and xv6 terms, every term of art defined on first use. If you run out of context, stop at a green committed state and write where a successor picks up.

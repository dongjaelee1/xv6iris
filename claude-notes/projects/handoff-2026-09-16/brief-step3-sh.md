# Lane brief (verbatim, as spawned 2026-09-14)

You are the agent for lane STEP3-SH of the xv6iris project (Rocq 9.0.1 / Iris weakest-precondition proofs of a RISC-V xv6 kernel with an "echo hello world" application theorem on top). YOUR CHECKOUT IS /shared/xv6iris-2-disc (absolute paths everywhere; never touch /shared/xv6iris-2, -sup, -tlw, -notes; never push; never edit claude-notes/ or iris/_CoqProject except to add a bare file row if you add a file -- you should not need to). Other lanes run concurrently in other checkouts: PROLOGUE-ALTS-3 (-tlw: EchoDisc/EchoOutPure/EchoOut/EchoLinks + new EchoLinksBan.v), INIT-DIAG (-sup: new EchoLinksPro.v/UInitDiag.v), and the coordinator does the INIT SIDE of the same step in the main checkout (UInitFd, UkInit, UkInitMain, UInitKernel, UInitBanner, UserConsole's `uinit_lend_c`). DO NOT edit those init-side files (UInitFd.v, UkInit.v, UkInitMain.v, UInitKernel.v, UInitBanner.v) at all; in UserConsole.v add ONLY the one lemma named in the contract.

SETUP: `cd /shared/xv6iris-2-disc && git fetch origin && git checkout -B lane/step3-sh origin/main` (origin/main = b7c49dea2 or later; the checkout is clean on a stale branch, abandon it). Confirm `git log --oneline -3`. Your VM tree is /mnt/rocq/trees/_shared_xv6iris-2-disc.

READ FIRST, IN FULL: /tmp/claude-0/-shared-xv6iris-2/2c386d62-e2e4-4d9f-b3c5-841bab14908e/scratchpad/step3-interface.md (THE BINDING CONTRACT for this lane -- every name and statement in it; your sections are "UkSh (lane S)", "Consumers of UkSh (lane S)", "The entry (lane S ...)", "UInitSh / UInitBoot (lane S: MINIMAL ...)"); claude-notes/README.md; claude-notes/durable-notes.md (whole file); claude-notes/projects/checkpoint-2026-09-16.md; claude-notes/projects/handoff-2026-09-16/io-leaf-handover.md (the "STEP 3 -- THE DESIGN OF RECORD" section at the top and "THE REST OF THE LANE"); claude-notes/projects/app-echo.md notes "IO-LEAF M6a(3) STEPS 1-2 LANDED", "IO-LEAF M6a(3) REVIEWED", "SH-LINE-CRED LANDED", "IO-LEAF WRITE-CLOSED LANDED", "SH-LINE RULING", "IO-LEAF M5(3)" (grep them). THE CODE, in this order: iris/UkSh.v (the section from `Context (Wc` at ~1485 through `ush_gets_done_set` at ~2110 in full, then `ush_pstate`/`ush_loop_head`/`ush_rest_l` at ~6439-6620, the gets loop's exit sites at ~3380, ~3583-3591, ~3818-3841, ~4013, getcmd at ~5095-5300, main's exit at ~7140-7150; and every use of `ush_prompt_in`/`ush_promptw`/`ksh_w_of_prompt_in`/`ush_pm_of_at`/`ush_at_of_pm`/`ush_posb*`/`ush_lease*` -- grep); iris/UkWriteClosed.v (`ksh_w_of_closed`, `ksh_w_of_closed_l0`); iris/UShLine.v (~483-620: `ush_rd_pin`, `ush_mid`, `ush_mid_of_at`, `ush_at_of_mid`, `ush_at_of_mid_taint`, `ush_mid_wc_read`, `ush_posb_of_at`; and `ush_read_recv_leaf_holds` at ~1128); iris/UShKernel.v (~400-420 `sh_prompt_at`, ~436 `sh_prompt_law`, `sh_uexec_slot` ~470-700, `sh_slot_of_kexec` ~710-800, `wp_ksh_start`); iris/UShOut.v (`sh_prompt_pay*`, `ksh_w_of_link_cred`, `sh_prompt_law_holds`); iris/UkShLoop.v (whole, small); iris/UkShCd.v, UkShFork.v, UkShEcho.v, UkShRun.v, UkShDiag.v, UkShMain.v (grep `Wc`, `ush_posb`, `ush_pstate`, `ush_at`, `ush_lease`); iris/UInitSh.v (`sh_pay_rest` ~513, `sh_pay` ~521, `sh_pay_of_parts`, `ufd_head_rows` ~897, `init_exec_sup_of_sh_slot` ~917-1100); iris/UInitBoot.v (`init_cons_sup_of_sh_slot` ~375-440 and `echo_Hinit_boot`'s assembly ~690-880); iris/UserConsole.v (`ucons_pay`, `ucons_pay_tok`, `ucons_pay_taint`, `uinit_tok`, `uinit_lend`, `uinit_redeem`); iris/UkInit.v `init_exec_sup_pos` (~1640-1670, READ ONLY -- you must not change it).

THE TASK (plain terms). The shell's command loop today carries, at every line boundary, its cursor bundled with its exit payload (`UkSh.ush_at n := upos γp n ∗ ukn_pay N (-1)`) and a write credential `Wc n p` that is either present (with the row "fd 2 is the console") or absent (`∨ True`). Step 3 UNBUNDLES the loop: the loop holds the lease's PIECES `Pm n` (opaque; `UShLine.ush_mid` is the instance) and assembles the payload only where it exits; the credential slot `ush_wcp l n p` gets three arms -- the BOTH-CONSOLE arm (fd 0 and fd 2 are the console: `Wc n p`), the CLOSED arm (fd 2 closed: a banner-owed credential `Wb n`, a NEW opaque section parameter, carried unchanged through the prompt because the prompt writes nothing on a closed descriptor -- `UkWriteClosed.ksh_w_of_closed`), and the affine `True` arm that step 4 kills. The exit payload's family becomes `Rd n := Rdl n ∗ (Wb n ∨ True)` (spelled `UkInit.init_rd Rdl Wb` on the init side; in YOUR files it appears through `UShLine.ush_rd_x γ Wb`; if `UShLine` cannot import `UkInit` because of the import graph, define `ush_rd_x γ Wb n := ush_rd_pin γ n ∗ (Wb n ∨ True)` locally and SAY SO in the report -- the coordinator will unify). sh's ENTRY takes the raw pieces plus the credential slot instead of the old affine prompt credential (`sh_prompt_at`/`sh_prompt_pay`/`ush_prompt_in` all die). Everything is spelled out in step3-interface.md; follow it to the letter, and where it is silent make the smallest choice consistent with it and REPORT the choice. Keep `init_exec_sup_of_sh_slot`/`init_cons_sup_of_sh_slot`/`echo_Hinit_boot` compiling against the UNCHANGED init side exactly as the contract's UInitSh/UInitBoot paragraph says (placeholder instantiations `Wb := fun _ => True%I`, the lease converted by your new `UserConsole.ucons_pay_mono`).

RULES: everything fully proved; no Admitted; the only `∨ True` arms are the ones the contract names (the `ush_wcp` True arm, `ush_rd_x`'s `(Wb n ∨ True)`), every one with a one-line comment `(* AFFINE -- step 4 kills it *)`. Pure facts about literals by `vm_compute`/`reflexivity` in small closed lemmas, never on goals with variables (durable-notes). Do not rename anything the contract does not rename. Do not add prose comments to iris/_CoqProject. Where you delete a lemma, grep the whole tree for its users first (`grep -rn name iris/*.v`). Section-variable orders matter: after your edits run `About UkSh.ush_posb`, `About UkSh.ush_wcp`, `About UkSh.ush_pstate`, `About UkSh.ush_rest_l`, `About UkSh.ush_gets_done` (via rocq-warm or a scratch file) and put the exact binder lists in the report -- the coordinator's glue depends on them.

BUILD PROCEDURE (never deviate): edit loop with rocq-warm (claude-notes/rocq-warm.md; `./gcp-rocq/run-on-gcp -q --no-sync bash -lc 'cd /mnt/rocq/trees/_shared_xv6iris-2-disc/iris && /mnt/rocq/rocq-warm/rocq-warm check UkSh.v'` after a sync -- NOTE the sync (`./gcp-rocq/run-on-gcp` without `--no-sync`) deletes the .vo of every file you edited, so once you touch A.v, a warm check of B.v that imports A fails with "Cannot find a physical path bound to logical path A"; rebuild A first from the repo root: `./gcp-rocq/run-on-gcp -q bash -lc 'cd /mnt/rocq/trees/_shared_xv6iris-2-disc/iris && make -f CoqMakefile -j8 A.vo'`). Full builds: from /shared/xv6iris-2-disc, `./gcp-rocq/vmbuild.sh xv6iris-2-disc s3-1` (then s3-2, ...), DETACHED (`setsid nohup ./gcp-rocq/vmbuild.sh xv6iris-2-disc s3-1 > /tmp/claude-0/-shared-xv6iris-2/2c386d62-e2e4-4d9f-b3c5-841bab14908e/scratchpad/s3-1.out 2>&1 &`; poll for the `EXIT=` line, never sleep-loop in the foreground for long); errors via `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/s3-1.log'`. One build at a time in your tree; never start one before the previous log has its EXIT= line; never pattern-kill on the VM (other agents build there); a run-on-gcp sync IS a build. UkSh.v is ~7400 lines and takes minutes to check; batch its edits.

GATE before you report: EXIT=0 and zero ^Error; VM `make -f CoqMakefile -n` prints no `ROCQ compile`; `cd /mnt/rocq/trees/_shared_xv6iris-2-disc && make audit-only 2>&1 | grep -v '^make\|^cd ' | md5sum` = 57f7327206c4b276d05035342fea8ecf (the thirteen; if different, print the audit and explain every line -- `Hsh_owed`'s `sh_pay_rest` gains a `Wb` binder, which is a TRUSTED-STATEMENT change: report OLD/NEW verbatim); `python3 tools/lemma_diff.py --ref origin/main` with every GONE/CHANGED justified; no Admitted; `./gcp-rocq/run-on-gcp --check-dumps` clean. Commit by explicit path on lane/step3-sh (author = the checkout's config; never `-c user.email`; end the message with `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01VzSVeaFhoeWNGQosZ6V4gt`). Never `git stash`, `git add -A`, `git reset`, `git commit -a`, `--amend`. Land green milestones as separate commits if natural (UkSh core; consumers + entry; UInitSh/UInitBoot placeholders), else one.

REPORT (final answer; also to /tmp/claude-0/-shared-xv6iris-2/2c386d62-e2e4-4d9f-b3c5-841bab14908e/scratchpad/step3-sh-report.md): the commit hashes; every new/changed lemma and definition statement VERBATIM (especially `ush_wcp`, `ush_posb`, the five hypotheses, `ush_gets_done_0/_line`, `ush_posb_of_lend`, `sh_uexec_slot`'s and `sh_slot_of_kexec`'s full premise lists, `init_exec_sup_of_sh_slot`'s binder list and Coq-level premises, `sh_pay_rest`'s OLD/NEW); the `About` binder lists asked for above; every deleted name and where its users went; every `∨ True` arm left and the step-4 obligation it stands for; the gate lines; anything the contract got wrong or you had to decide. Plain concurrent-separation-logic and xv6 terms, every term of art defined on first use. If you run out of context, stop at a green committed state and write where a successor picks up.
# Lane brief (verbatim, as spawned 2026-09-14)

You are the agent for lane PROLOGUE-ALTS-3 of the xv6iris project (Rocq/Iris weakest-precondition proofs of a RISC-V xv6 kernel with an "echo hello world" application theorem on top). YOUR CHECKOUT IS /shared/xv6iris-2-tlw (absolute paths; never touch /shared/xv6iris-2, -disc, -sup, -notes; never push; never edit claude-notes/). Two other lanes are adding NEW files that require iris/EchoLinks.v (iris/EchoLinksPro.v in -sup, iris/EchoLinksLine.v in -disc); they will rebase onto your change, so keep every EXISTING lemma statement in EchoDisc/EchoOutPure/EchoOut/EchoLinks derivable (restate the old name as a corollary where a statement must generalise -- durable-notes, "Generalizing a lemma without churning call sites"). Do NOT edit any U-tier program file (iris/Uk*.v, iris/USh*.v, iris/UInit*.v, iris/UEcho*.v) except where a changed pure lemma statement forces a one-line fix; list every such line.

SETUP: `cd /shared/xv6iris-2-tlw && git fetch origin && git checkout -B lane/prologue-alts-3 origin/main` (origin/main = 6fb3dbaa6 or later; the checkout is clean on a stale branch `lane/open-row`, abandon it). Confirm `git log --oneline -3`. Your VM tree /mnt/rocq/trees/_shared_xv6iris-2-tlw is at an older commit; the first build rebuilds a large cone.

READ FIRST, IN FULL: claude-notes/README.md; claude-notes/durable-notes.md (whole file); claude-notes/projects/checkpoint-2026-09-16.md (§5 rulings, including the 2026-09-14 ruling THE BANNER IS OPTIONAL); claude-notes/projects/app-echo.md: the notes "OPEN-ROW WITHDRAWN -- RULED 2026-09-14", "OPEN-ROW FOUND", "IO-LEAF M6a(3) REVIEWED" (Q3), "IO-LEAF M6a(3) STEPS 1-2 LANDED", "IO-LEAF M6a(2) LANDED", "IO-LEAF M6a(1) LANDED", "PROLOGUE-ALTS LANDED", "PROLOGUE-ALTS-2 LANDED", "ECHO-PURE LANDED", "ECHO-OUT" parts (grep), and the "E5 -- THE CONSOLE I/O CLAIM: DESIGN OF RECORD" section; claude-notes/projects/handoff-2026-09-16/prologue-alts-handover.md and prologue-alts-2-handover.md (the moulds for exactly this kind of change: they added alternatives 1 and 2 and the arbitrary-round banner). THE CODE: iris/EchoDisc.v (all of the prologue section: `u_banner`, `u_prompt`, `pro_alts`, `pro_more`, `pro_of`, `pro_done`, `pro_tail`, `pro_from`, `pro_rounds`, `pro_round`, `pro_of_good`, `pro_of_banner`, `pro_of_pos`, `pro_of_snoc`, `pro_of_mono`, `pro_of_done_ext`, `pro_of_prefix_free`, `pro_alts_nonnil`, `pro_alts_head*`, the session `sess_n`/`sess`/`alt_seq`/`alt_blk`, `pro_ok`, `pro_pin`, `pro_idx`, `expected_rel`, `disc_pt`, `disc_seg'`, `disc`, `good_out`, `disc_pt_good_out_pin`, and every lemma that mentions `pro_alts`/`pro_of`); iris/EchoOutPure.v (the stage machine: `proc_upto` (find it -- it may be a Notation or live under another name; grep `proc_upto` tree-wide), `pending_n`, `pending`, `pro_pin_*`, `pro_idx_*`, `proc_upto_round_banner_open`, `pro_of_open_snoc_eq`, `pending_n_round_shape/_snoc`, `proc_upto_gap`, everything PROLOGUE-ALTS/-2 added); iris/EchoOut.v (`eout`, `ein`, `Elist_auth`/`Elist_lb`/`E_lb`, `turn`, `ps_lb`, `cs_lb`, `eout_step_write`, `eout_step_write_blk`, `eout_step_write_pro`, `echo_write_link`/`_blk`/`_pro` at ~3321, `ein_step_read`, `ein_read_byte`, `read_ret`, the claim's invariant relating the log `E` to the wire/stage); iris/EchoLinks.v (all: `wr_pro`/`wr_blk`/`wr_open`/`wr_ban`/`wr_sp`/`wr_pre`, their steps, `ewc_*`, `echo_banner_step`, `ewc_ban_done`, `echo_prompt_dollar`/`_space`, `ewc_read`, `ewc_pr`/`ewc_cred`, `echo_links`); iris/UInitBanner.v (`kinit_ban`, `kinit_own`, `kinit_banner_law_holds`); iris/UShOut.v (`ushpr_step`, `ksh_w_of_link_prompt`) -- to know the consumers' shapes.

THE RULING AND THE TASK. The owner ruled (2026-09-14): init's banner is OPTIONAL in the transcript -- "a trace without a banner in a given era has to be possible anyway". The machine reason: <init>'s console open can fail (the kernel's open contract admits a table-full failure with no refutable reason) while the shell's own opens succeed; <init> then prints nothing and lends the shell the credential in its BANNER-OWED shape (`wr_ban`, nothing of the round on the wire), and the shell's "$ " is the round's first byte. So a prologue round may be "$ " alone. Deliver, as ONE consistent change through the pure layer, the claim and the links:
(1) EchoDisc: a FOURTH prologue alternative, index 3 = `u_prompt` ("$ ") written WITHOUT the banner. `pro_alts := [u_prompt; u_execfail; u_forkfail; u_prompt]`; `pro_of` prepends `u_banner` for entries 0-2 and NOT for 3 (e.g. `pro_head a := if decide (a = 3) then [] else u_banner`); alternative 3 completes a round exactly like 0 (`pro_done` is `a <> 1`, unchanged); the alternatives' ROUND STRINGS stay pairwise prefix-free (`u_banner ++ "$ "`, `u_banner ++ execfail`, `u_banner ++ forkfail`, `"$ "` -- byte 0 separates 3 from the rest, byte 6 separates 1 from 2), so `pro_of_prefix_free` and the resolution-off-the-wire argument survive; `pro_of_banner` (every prologue starts with the banner) becomes FALSE -- replace it by what its consumers need (find every consumer; the arbitrary-round banner lemma `proc_upto_round_banner_open` of PROLOGUE-ALTS-2 is one) and say what changed. The default for an UNRESOLVED round (`pro_of [] = u_banner`, "out of choices the prologue is the bare banner") is the subtle point: it is what `sess_n`/`proc_upto` predict before a round's first byte. Decide whether it stays (a wire that is empty or mid-banner is still a prefix of the expected transcript under the resolution that has not chosen yet, and a wire starting with '$' is a prefix under `ps ++ [3]`) or whether the unresolved round should predict nothing; keep `expected_rel`'s meaning ("the wire is a prefix of the transcript under SOME resolution") and prove `pro_of_good`, the monotonicity/extension lemmas and `disc_pt_good_out_pin` at the new definition. Every lemma quantified `Forall (a < length pro_alts)` now admits 3; check each proof.
(2) EchoOutPure/EchoOut: the stage machine at alternative 3. The banner-owed credential shape `EchoLinks.wr_ban ps cs n P` says "`j` failed sub-rounds in, the round's banner is owed at stage `P = |proc_upto ps cs n| + |wr_pre cs n| + pro_round * j`" (read its comment). Writing '$' from that shape (at `j = 0`; if `j > 0` the previous sub-rounds' bytes are on the wire and the banner-less prompt cannot follow -- say whether `j > 0` must be excluded or is admitted) files alternative 3 and the round's stream continues with ' '. Make `echo_write_link_pro` (EchoOut.v:~3321, the CHOICE-BYTE link, whose stage premise is `P = length (proc_upto ps0 cs0 (S n0))` -- the stage AFTER the open round's predicted banner) admit `a = 3` at the banner-less stage: either generalise its stage premise to a per-alternative stage function (`pro_stage ps cs n a`, equal to the landed expression for `a < 3`) with the old statement kept as a corollary, or add a twin `echo_write_link_pro3`; the claim-side step (`eout_step_write_pro`) and the pure facts behind it (`pro_of_open_snoc_eq`, `pending_n_round_snoc`, `proc_upto_gap`) are where the work is. Then in EchoLinks: the pure step `wr_ban_dollar : wr_ban ps cs n P (at j = 0) -> wr_sp (ps ++ [3]) cs n (S P)` (or whatever shape the ' ' step needs; `wr_sp`/`wr_open` may need `a = 3` cases) and the Iris step `echo_prompt_dollar_ban (k v n b Φ) : b = u_prompt !!! 0 -> era_pin γ k v -∗ echo_links -∗ ewc_ban v n 0 -∗ (ewc_sp v n -∗ Φ) -∗ out_link Uart0 k b Φ`, so that `echo_prompt_space` and `ewc_read` then carry the round on unchanged. Add `echo_links` a projection ONLY if a new link is genuinely needed; prefer generalising `echo_link_pro`.
(3) THE DISCIPLINE LEMMA (the closed-fd-2 artifact the ruling leaves): a claim-level fact in EchoOut that "an untainted input at line k implies the prompt of line k was written" -- precisely: with the writer holding `turn v P` at a stage where the current round's/line's prompt is NOT yet on the wire (`wr_owed`-shaped / `wr_ban`-shaped at boundary n), the read link's non-taint arm cannot deliver a byte at count n: state it as a lemma the shell can use, e.g. `E_lb v (S n) ∗ turn v P ∗ ⌜<the stage says the prompt for line n/17 is not written>⌝ ⊢ False`, or as an extra pure conjunct in `read_ret`'s non-taint arm relating `n + |ws|` to the choices/stage that `ewc_read`'s consumer can spend (the consumer's obligation: after a read at a boundary where the credential is still at p = 0 (prompt unwritten), refute the non-taint arm). Find the invariant conjunct that makes it true (the tag `⌜disc h⌝ ∨ T` on each log entry, `disc_pt`'s "the expected transcript for i inputs is a prefix of the wire at the history the byte was accepted", and the output claim's `acc = D cs E ++ w` relating the wire to the stage) and prove it; if the invariant does not record enough to prove it, say exactly what conjunct is missing and STOP on that item (do not weaken anything to make it go through).
(4) `UInitBanner`/`UShOut`/`EchoLinks` consumers: keep them compiling; the top-level instance of the shell's credential family will become `∃ v, era_pin γ k v ∗ (ewc_owed v n ∨ ewc_ban v n 0)` at p = 0 (the coordinator does that in the main checkout) -- you need not do it, but `ewc_pr`'s p = 0 arm may be widened to that disjunction here if it makes (2)'s statements cleaner; say so if you do.
Everything fully proved; no Admitted; no `∨ True`; pure facts about the literals by `vm_compute`/`reflexivity` in small closed lemmas, never on goals with variables (durable-notes). The trusted surface: `EchoDisc.pro_alts`/`pro_of`/`expected_rel` ARE the theorem's trace predicate -- report OLD/NEW of every changed definition among them verbatim.

BUILD PROCEDURE (never deviate): from /shared/xv6iris-2-tlw, `./gcp-rocq/vmbuild.sh xv6iris-2-tlw pa3-1` (then pa3-2, ...), DETACHED (`setsid nohup ... > <your scratchpad>/pa3-1.out 2>&1 &`; poll for `EXIT=`); errors via `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/pa3-1.log'`. One build at a time in your tree; never start one before the previous log has its EXIT= line; never pattern-kill on the VM (other agents build there); a run-on-gcp sync IS a build; rocq-warm (claude-notes/rocq-warm.md) only between builds and only for files whose dependencies have .vo on the VM. EchoDisc sits under the whole application layer (~60 files); batch the pure changes and build once.

GATE before you report: EXIT=0 and zero ^Error; VM `make -f CoqMakefile -n` prints no `ROCQ compile`; `cd /mnt/rocq/trees/_shared_xv6iris-2-tlw && make audit-only 2>&1 | grep -v '^make\|^cd ' | md5sum` = 57f7327206c4b276d05035342fea8ecf (the thirteen; if different, print the audit and explain every line); `python3 tools/lemma_diff.py --ref origin/main` with every GONE/CHANGED justified; no Admitted; `./gcp-rocq/run-on-gcp --check-dumps` clean. Commit by explicit path on lane/prologue-alts-3 (author = the checkout's config; never `-c user.email`; end the message with `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01VzSVeaFhoeWNGQosZ6V4gt`). Never `git stash`, `git add -A`, `git reset`, `git commit -a`, `--amend`. Land green milestones as separate commits ((1), (2), (3)).

REPORT (final answer; also to /tmp/claude-0/-shared-xv6iris-2/2c386d62-e2e4-4d9f-b3c5-841bab14908e/scratchpad/prologue-alts-3-report.md): the commit hashes; the TRUSTED DIFF (`pro_alts`, `pro_of`, `expected_rel`, `disc_pt` or anything else in EchoDisc's predicate) OLD/NEW verbatim; every new or changed lemma statement verbatim, especially `echo_write_link_pro`'s (or its twin's) and the discipline lemma's; which existing lemmas went GONE/CHANGED and why; the one-line fixes outside the four files; the gate lines; what the coordinator receives for step 3 (the exact Iris lemma to pay the shell's prompt from the banner-owed credential, and the exact lemma to refute an untainted read at an unwritten prompt). Plain concurrent-separation-logic and xv6 terms, every term of art defined on first use. If you run out of context, stop at a green committed state and write where a successor picks up.
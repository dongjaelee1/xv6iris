You are the agent for lane RESIDUALS of the xv6iris project (Rocq 9.0.1 / Iris weakest-precondition proofs of a RISC-V xv6 kernel with an "echo hello world" application theorem on top). YOUR CHECKOUT IS /shared/xv6iris-2-disc (absolute paths everywhere; never touch /shared/xv6iris-2, -sup, -tlw, -notes; never push; never edit claude-notes/; iris/_CoqProject gets a bare file row only if you add a file). Two READ-ONLY lanes run concurrently elsewhere; you are the only lane editing code, and you may edit both the shell files and the init files.

SETUP: `cd /shared/xv6iris-2-disc && git fetch origin && git checkout -B lane/residuals origin/main` (origin/main = 7da574e81 code + notes commits; the checkout is clean on lane/step4-sh which is merged). Confirm `git log --oneline -3`. Your VM tree is /mnt/rocq/trees/_shared_xv6iris-2-disc.

READ FIRST, IN FULL: claude-notes/README.md; claude-notes/durable-notes.md (whole); claude-notes/projects/checkpoint-2026-09-14-step3.md; claude-notes/projects/checkpoint-2026-09-16.md §6 (build/gate/commit rules); claude-notes/projects/app-echo.md notes dated 2026-09-14 "IO-LEAF STEP 4 LANDED", "M6b LANDED", "IO-LEAF STEP 3 LANDED", "DIE-DW CORRECTED"; claude-notes/projects/handoff-2026-09-16/step4-sh-report.md (WHOLE -- §4 "Every ∨ True left", §5 "Remaining spenders of sh_deps", §7 are your work list) and m6b-init-report.md ("Every ∨ True left in init's files", "Spenders of udepw_law 16"); step3-interface.md (vocabulary). THE CODE you will need: iris/UkSh.v (`ush_wcp`, `ush_wcp_triv`, `ush_wcp_cons`, `ksh_w_of_wcp`, `ush_posb*`, `ush_gets_done*` -- `ush_gets_done_0`'s affine arm, `ush_gets_done_line`'s affine arm, `sh_deps`, `wp_ksh_getcmd`'s taint arm, `wp_ksh_start`), iris/UkShFork.v (the fork arm: the console arm's PID sub-arm at `sign_extend' 64 pidv = -1`, the affine/taint arm running the GENERIC child `UkShMain.wp_kshm_child_alloc`, the wait re-entry's "not the forked generation" arm, `ushf_rest_of_body`), iris/UkShDiag.v (`wp_kshr_fork1_final` -- the fork answer's shape: it DROPS the pid-range row), iris/UkShRun.v (`wp_kshr_fork1`, `wp_kshr_wait_pid`), iris/UkFork.v (`wp_uk_ecall_fork` ~784: its parent post already carries `⌜1 <= bv_unsigned pidv <= PIDMAX⌝` at ~236 -- the pid-range fact EXISTS on the leaf), iris/UexecRet.v (`uwait_ans_pid_mine` ~1087, `uwait_ans_pid`), iris/UkRunSys.v (`wp_uk_ecall_wait_null_pid`), iris/SpecKexec.v (`exec_slot_pre` ~799), iris/PinnedExec.v (`pinned_exec_bundle`; grep `uvis_ch`/`uvis_pid`/`upid`), iris/UkRun.v (`uvis`, `uvis_ch`, `uvis_pid`, `urun`'s fields), iris/UkInitMain.v (`wp_kinit_fork`'s child arm ~940-1000: what the child's facts are and what is DROPPED with `_` -- the step-4 report says UkInitMain:954 drops `∃ p, ⌜p <> 1⌝ ∗ upid`), iris/UInitSh.v (`init_exec_sup_of_sh_slot`: how `init_lend_cred`'s affine arm is mapped to `ush_wcp`'s third arm; how the child's pid/children facts reach sh's entry `sh_uexec_slot`), iris/UShKernel.v (`sh_uexec_slot`, `sh_slot_of_kexec`, `wp_ksh_start`: what facts about `W` -- `uvis_ch W`, `uvis_pid W` -- the entry has), iris/UkInit.v (`init_rd_cred`, `init_rd`, `init_lend_cred`, `init_lend_cred_triv`, `init_pay_of_lend`), iris/UkInitMain.v (`wp_kinit_banner` ~1100-1130: where `init_lend_cred_triv` is built from `init_rd_cred`'s True arm), iris/UShLine.v (`ush_rd_x`, `ush_at_of_mid`, `ush_at_of_mid_wb`, `ush_posb_of_lend`), iris/UInitBoot.v (`echo_Hinit_boot`: the `Hsh_*` asserts; `init_deps_of_sup`/`Htsw`/`Hstw` -- how init's free law is derived from the app supply and the taint), iris/UInitBootAdequacy.v (`Hsh_owed`, the TRUSTED top statement).

THE TASK: kill every remaining affine arm (`∨ True`) in the shell's and init's files, and then make the shell's free write law `sh_deps` unnecessary on the paid walk so that `Hsh_owed`'s FIRST conjunct `(⊢ UkSh.sh_deps (PS := uprogSG_free))` can be deleted from the top theorem. Plain terms: an affine arm lets a proof throw a credential away; each one still standing marks a path where the shell's exit hands init back nothing, and init then prints its next banner through the "free" write law -- a placeholder that counts a console write as paid without the era's credential. The step-4 report lists them and their killers; do them in this order, each a separate green commit:
(A) THE PID SUB-ARM. `UkShFork.wp_kshf_fork`'s console arm, fork answer `sign_extend' 64 pidv = -1` (pidv = 0xFFFFFFFF): thread the pid-range row `⌜1 <= bv_unsigned pidv <= PIDMAX⌝` from `UkFork.wp_uk_ecall_fork`'s parent post through `UkShRun.wp_kshr_fork1` / `UkShDiag.wp_kshr_fork1_final` (their answer shapes drop it) to the fork arm, and REFUTE the sub-arm (a pid in [1, PIDMAX] sign-extends to a small positive: `UexecRet.v:1086` has the argument; ProcGeom's PIDMAX). No kernel contract changes.
(B) THE EXEC-SEAM ROWS. The wait re-entry's "the reaped generation is not the forked one" arm: the shell's own exec'd key carries `uvis_ch W' = ∅` (a freshly exec'd sh has no children) and `uvis_pid W' = pidv` for the pid init's fork answered (and `pidv <> 1`); these facts exist at init's fork child (UkInitMain ~954 drops them) and must be threaded through init's exec supply (`UkInit.init_exec_sup_pos`/`init_lend_ref`, `UInitSh.init_exec_sup_of_sh_slot`) into sh's entry (`UShKernel.sh_uexec_slot`) and its loop state (`UkSh.ush_pstate` carries `upid_any` since step 4 -- give it the pid), so that `UkShRun.wp_kshr_wait_pid` + `uwait_ans_pid_mine` make the reaped child the forked one and the arm disappears. Check FIRST what `SpecKexec.exec_slot_pre`/`PinnedExec.pinned_exec_bundle` say about the exec'd image's children set and pid (grep); if the seam does NOT carry them, say exactly what is missing and stop on (B) (do not weaken anything), then continue with (C) for the arms that do not depend on it.
(C) THE KILLS. With every shell exit handing back `Wb n` (the shut-fd-0 exit, the fork panic, and after (A)/(B) nothing else remains but the taint), make `UkInit.init_rd_cred Wb n := Wb n` (delete the `∨ True`; `UShLine.ush_rd_x` follows; `ush_at_of_mid` (the right-arm assembler) and `UkSh.ush_at_of_pm` die -- every assembly goes through `ush_at_of_mid_wb`/`ush_at_of_pm_wb` or the taint), make `UkInit.init_lend_cred`'s third arm `T` (in `wp_kinit_banner` the only credential-less case left is the taint row), delete `UkSh.ush_wcp`'s third arm and `ush_wcp_triv` and `ush_gets_done_0`/`ush_gets_done_line`'s affine arms, and `UInitSh`'s mapping of the affine arm. Every `∨ True` then gone from UkSh/UkSh*/USh*/UkInit*/UInit* -- grep and list any survivor with its reason.
(D) THE FREE LAW. After (C), `sh_deps` is spent on the paid walk ONLY on taint arms (getcmd's, the fork's taint arm running the generic child) and in the GENERIC runner/diagnostic lemmas that the paid child never enters. Restate the shell's need as `□ (T -∗ UkSh.sh_deps)` (or the equivalent per-row wands) wherever the paid walk still spends it, derived at the top from the application supply exactly as init's `init_deps_of_sup` derives init's (`app_sup ⊣⊢ echo_taint γ`, UInitBoot's `Htsw`/`Hstw`), leave the generic lemmas' unconditional premise as it is (they are not on the paid path), remove `sh_deps` from `UShKernel.sh_uexec_slot`/`sh_slot_of_kexec`/`UInitSh.init_exec_sup_of_sh_slot`/`sh_pay`/`init_sh_slot` where it was only forwarded, and DELETE the first conjunct of `Hsh_owed` in iris/UInitBootAdequacy.v (and `UInitBoot.echo_Hinit_boot`'s corresponding premise). TRUSTED STATEMENT CHANGE: report `Hsh_owed`'s OLD/NEW verbatim, and the audit md5 WILL CHANGE if the theorem's hypothesis count changes -- print the audit and explain every line.

RULES: everything fully proved; no Admitted; no new `∨ True`; pure facts about literals by `vm_compute`/`reflexivity` in small closed lemmas, never on goals with variables; every file must `Require` what it names; name Timeless/Persistent instance binders explicitly; do not rename what you need not; grep the whole tree for users before deleting; no prose in iris/_CoqProject; the audit md5 57f7327206c4b276d05035342fea8ecf is the thirteen ambient assumptions -- any change to it must be explained line by line (the only expected change is (D)'s).

BUILD PROCEDURE (never deviate): edit loop with rocq-warm (claude-notes/rocq-warm.md): `./gcp-rocq/run-on-gcp -q bash -lc 'cd /mnt/rocq/trees/_shared_xv6iris-2-disc/iris && /mnt/rocq/rocq-warm/rocq-warm check UkShFork.v'` (the sync deletes the .vo of every file you edited; rebuild an edited dependency first with `make -f CoqMakefile -j8 A.vo` in the VM tree; a stale intermediate gives "makes inconsistent assumptions" -- make its .vo too). Full builds: from /shared/xv6iris-2-disc, `./gcp-rocq/vmbuild.sh xv6iris-2-disc rs-1` (then rs-2, ...), DETACHED (`setsid nohup ./gcp-rocq/vmbuild.sh xv6iris-2-disc rs-1 > /tmp/claude-0/-shared-xv6iris-2/2c386d62-e2e4-4d9f-b3c5-841bab14908e/scratchpad/rs-1.out 2>&1 &`), then POLL the .out file for its `EXIT=` line with short sleeps from your own turn (a detached build does not wake you); errors via `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/rs-1.log'`. One build at a time in your tree; never start one before the previous log has its EXIT= line; never pattern-kill on the VM; a run-on-gcp sync IS a build.

GATE before you report (per commit): EXIT=0 and zero ^Error; VM `make -f CoqMakefile -n` prints no `ROCQ compile`; `cd /mnt/rocq/trees/_shared_xv6iris-2-disc && make audit-only 2>&1 | grep -v '^make\|^cd ' | md5sum` (57f7327206c4b276d05035342fea8ecf until (D); after (D) print the audit); `python3 tools/lemma_diff.py --ref origin/main` with every GONE/CHANGED/NEWAXIOM justified; no Admitted; `./gcp-rocq/run-on-gcp --check-dumps` clean. Commit by explicit path on lane/residuals (author = the checkout's config; never `-c user.email`; end the message with `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01VzSVeaFhoeWNGQosZ6V4gt`). Never `git stash`, `git add -A`, `git reset`, `git commit -a`, `--amend`.

REPORT (final answer; also to /tmp/claude-0/-shared-xv6iris-2/2c386d62-e2e4-4d9f-b3c5-841bab14908e/scratchpad/residuals-report.md): commit hashes per milestone; every changed statement verbatim (the fork answer's shape, the seam rows and where they come from, `init_rd_cred`/`init_lend_cred`/`ush_wcp` NEW, `Hsh_owed` OLD/NEW, `sh_uexec_slot`'s premise list); every deleted name and where its users went; every `∨ True` left anywhere in iris/Uk*.v iris/USh*.v iris/UInit*.v iris/UEcho*.v with its reason; every remaining spender of `sh_deps` and of `udepw_law 16`; the gate lines per commit including the audit text after (D); anything the brief did not settle. Plain concurrent-separation-logic and xv6 terms, every term of art defined on first use. If you run out of context, stop at a green committed state and write where a successor picks up.

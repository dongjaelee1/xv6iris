# Lane brief (verbatim, as spawned 2026-09-14)

You are the agent for lane INIT-DIAG (M6b preparation) of the xv6iris project (Rocq/Iris weakest-precondition proofs of a RISC-V xv6 kernel with an "echo hello world" application theorem on top). YOUR CHECKOUT IS /shared/xv6iris-2-sup (absolute paths; never touch /shared/xv6iris-2, -disc, -tlw, -notes; never push; never edit claude-notes/). Other agents are concurrently editing iris/EchoLinks.v, iris/UShOut.v, iris/UInitBanner.v, iris/UkInit*.v, iris/UkInitMain.v, iris/UInitKernel.v, iris/UInitSh.v, iris/UInitBoot.v, iris/UShKernel.v, iris/UkSh*.v, iris/UShLine.v, iris/UserConsole.v elsewhere: you must NOT edit any existing iris/*.v file. Your work goes in NEW files only (plus one row each in iris/_CoqProject, placed right after the row of the highest file they require).

SETUP: `cd /shared/xv6iris-2-sup && git fetch origin && git checkout -B lane/init-diag origin/main` (origin/main = c3fb395bd or later: it has `iris/UkWriteClosed.v`, `EchoLinks.ewc_pr`/`ewc_cred`, the M6a(3) landing). Confirm `git log --oneline -3`. Your VM tree /mnt/rocq/trees/_shared_xv6iris-2-sup is at a43341d28 + UkWriteClosed; the first build will rebuild the M6a(3) cone (~30 files).

READ FIRST, IN FULL: claude-notes/README.md; claude-notes/durable-notes.md (whole file); claude-notes/projects/checkpoint-2026-09-16.md; claude-notes/projects/app-echo.md -- the notes headed "IO-LEAF M6a(3) REVIEWED", "IO-LEAF M6a(3) STEPS 1-2 LANDED", "IO-LEAF M6a(2) LANDED", "IO-LEAF M6a(1) LANDED", "PROLOGUE-ALTS LANDED", "PROLOGUE-ALTS-2 LANDED", "IO-LEAF M1(c,d)", "IO-LEAF M1(e)", "IO-LEAF M1(f)" (grep for them) and the "E5 -- THE CONSOLE I/O CLAIM: DESIGN OF RECORD" section; claude-notes/projects/handoff-2026-09-16/endgame-audit.md (the PAYABLE SITES paragraph: "`echo_link_pro` a=1/2 (init's exec/fork failures)"; the `∨ True` inventory for init); claude-notes/projects/handoff-2026-09-16/io-leaf-handover.md ("/INIT'S ARMS AFTER (A)"). THE CODE: iris/EchoDisc.v (`u_banner`, `u_execfail`, `pro_alts`, `line_alts`, `pro_round`, `echo_line`, `u_prompt`); iris/EchoOutPure.v (`proc_upto`, `pro_of`, `pro_pin`, `pro_done`, `pro_from`, `pro_idx`, `pro_rounds`, `pending_n`, `proc_upto_round_banner_open`, and everything PROLOGUE-ALTS/-2 added); iris/EchoOut.v (`turn`, `ps_lb`, `cs_lb`, `E_lb`, `era_pin`, `echo_write_link`, `echo_write_link_blk`, `echo_write_link_pro`); iris/EchoLinks.v (ALL of it: the four pure shapes `wr_pro`/`wr_blk`/`wr_open`/`wr_ban` and their steps, `ewc_owed`/`ewc_sp`/`ewc_open`/`ewc_ban`/`ewc_pr`/`ewc_cred`, `echo_banner_step`, `ewc_ban_done`, `echo_prompt_dollar`, `echo_prompt_space`, `ewc_read`, the six projections `echo_links_w/_blk/_pro/_taint/_rd/_rd_taint`); iris/UInitBanner.v (the mould for everything you write: `bnr`, `kinit_w1_of_link`, `kinit_ban`/`kinit_own`/`kinit_dl0`, `kinit_ban0_of_eturn`, `kinit_banner_law_holds`, `kinit_banner0_mono`, `sh_prompt_pay_of_kinit_own`, `kinit_ban_any`, `kinit_prompt_law_holds`); iris/UkInit.v (`kinit_w1`, `kinit_w1_of_law`, `kinit_banner_pay`, `init_deps`, the die arms: grep `die_de`, `die_df`, `die_dw`, `wp_kinit_die`, and the putc/printf tower `UkInitPutc.v`/`UkInitPrintf.v`/`UkInitVprintf.v` -- how init's "init: exec sh failed\n" and "init: fork failed\n" are written today and what per-byte family, if any, they take; `UkInitMain.v`'s `kinit_banner0`, `kinit_ban_law`, `kinit_round0`, and the die sites `wp_kinit_main_child`'s failing-exec arm and `wp_kinit_fork`'s -1 arm); iris/UkWriteClosed.v (`kinit_w1_of_closed`).

THE TASK (lemmas only; nothing wired into init's walk -- that is M6b proper, after step 3 of M6a(3) restructures init's lend): init's two payable failure diagnostics are prologue alternatives -- `pro_alts !!! 1` = "init: exec sh failed\n" (the exec-failed CHILD prints it and exits; init reaps and prints its banner again: the round gets one more failed sub-round `j`, which is `wr_ban`'s `replicate j 1` and `pro_round * j` offset) and `pro_alts !!! 2` = "init: fork failed\n" (terminal: init exits, nothing follows). Deliver, in NEW files `iris/EchoLinksPro.v` (requires EchoLinks; `_CoqProject` row right after `EchoLinks.v`) and `iris/UInitDiag.v` (requires UInitBanner and UkWriteClosed if useful; row right after `UInitBanner.v`), in EchoLinks's own section mould (`Context (T) (γ)`, `Persistent T`, `Timeless T`, `riscvGS`):
(1) THE ROUND-OPEN CREDENTIAL, EXACTLY: what init's banner leaves behind is `wr_pro`-shaped (`wr_ban_pro`/`ewc_ban_done` end there), but `kinit_own n` is stated at `ewc_owed = wr_pro ∨ wr_blk`. Define `ewc_pro v n := (∃ ps cs P, ⌜wr_pro ps cs n P⌝ ∗ turn v P ∗ ps_lb v ps ∗ cs_lb v cs ∗ E_lb v n) ∨ T`, prove `ewc_owed_of_pro : ewc_pro v n -∗ ewc_owed v n` and `ewc_ban_done_pro : ewc_ban v n (length u_banner) -∗ ewc_pro v n` (from `ewc_ban_done`'s proof / `wr_ban_pro`), and `ewc_pro_of_owed_round_start` if `wr_blk` is refutable from a fact init holds at a round start (say which fact; if none, say so -- the point is whether init can get `wr_pro` back out of `ewc_owed` after lending and getting the credential refunded on the fork's -1 arm: `FORK-REFUND` returns `Rc` whole, so if `Rc` carries `ewc_pro` there is nothing to refute; state that clearly).
(2) THE DIAGNOSTIC'S BYTES THROUGH THE LINKS: for `a ∈ {1, 2}`, the family `ewc_pdiag v n a i` ("the round's choice `a` filed with `i` of its bytes out": byte 0 goes through `echo_link_pro` at `a`, which files `ps ++ [a]`; bytes `1 ..` through `echo_link_w`; work out the exact pure shape after byte 0 from `wr_pro_dollar`'s proof, which is the same step at `a = 0`) with `ewc_pdiag_0 : ewc_pro v n -∗ ewc_pdiag v n a 0` (or the definitional equality), `echo_pdiag_step a i b : pro_alts !!! a !! i = Some b -> era_pin -∗ echo_links -∗ ewc_pdiag v n a i -∗ (ewc_pdiag v n a (S i) -∗ Φ) -∗ out_link Uart0 k b Φ` (taint arm through `echo_links_taint` as `echo_banner_step` does), and the END SHAPES: `ewc_pdiag_done_1 : ewc_pdiag v n 1 (length (pro_alts !!! 1)) -∗ ewc_ban v n 0` (the next sub-round's banner is owed at the SAME count `n`, with one more failed sub-round: check `wr_ban`'s `∃ j` conjunct and `pro_round`'s arithmetic -- `pro_round = 39 = |u_banner| + |pro_alts !!! 1|`; `pro_of_replicate_length` and `proc_upto_round_banner_open` are the tools) and for `a = 2` nothing (the round is terminal; just note that the credential is dropped, affine).
(3) INIT'S PER-BYTE OBLIGATION AT THE DIAGNOSTIC: `kinit_w1_of_link_pdiag N v n a i b : l !! 1 = Some (FdOpen rb true (FdDevice CONSOLE)) -> pro_alts !!! a !! i = Some b -> era_pin -∗ echo_links -∗ UkInit.kinit_w1 N 1 b (ustd l ∗ pdg v n a i) (ustd l ∗ pdg v n a (S i))` on `UInitBanner.kinit_w1_of_link`'s exact mould (`pdg := ewc_pdiag T`), and -- IF init's die sites are written over a per-byte family the way the banner is (`UkInit.kinit_banner_pay`'s `Ch`) -- the law `kinit_pdiag_law_holds a : echo_links -∗ □ (∀ n N, <ewc_pro-shaped credential with the pin> -∗ <the die site's payment producing the end shape>)` on `kinit_banner_law_holds`'s mould. If the die sites are NOT yet threaded with a per-byte family (they may still spend `init_deps`' `udepw_law 16` through the printf tower at a `%s`-free literal), do NOT thread them (that is M6b proper, in UkInit*/UkInitMain which you may not edit): state in your report exactly which lemma in UkInit/UkInitMain writes each diagnostic, what family it would need, and stop after (1)-(3)'s per-byte lemma.
Everything fully proved; no Admitted; no `∨ True` placeholders; pure facts about the literals by `vm_compute`/`reflexivity` in small closed lemmas as EchoLinks does (never `vm_compute` on a goal with variables -- durable-notes).

BUILD PROCEDURE (never deviate): from /shared/xv6iris-2-sup, `./gcp-rocq/vmbuild.sh xv6iris-2-sup id-1` (then id-2, ...), run DETACHED (`setsid nohup ... > <your scratchpad>/id-1.out 2>&1 &`, poll for `EXIT=`); errors via `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/id-1.log'`. One build at a time in your tree; never start one before the previous log has its EXIT= line; never pattern-kill on the VM; a run-on-gcp sync IS a build; rocq-warm (claude-notes/rocq-warm.md) only between builds and only once your files' dependencies have .vo on the VM.

GATE before you report: EXIT=0 and zero ^Error; VM `make -f CoqMakefile -n` prints no `ROCQ compile`; `python3 tools/lemma_diff.py --ref origin/main` CLEAN; no Admitted; `./gcp-rocq/run-on-gcp --check-dumps` clean. Commit by explicit path on lane/init-diag (author = the checkout's config; never `-c user.email`; end the message with `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01VzSVeaFhoeWNGQosZ6V4gt`). Never `git stash`, `git add -A`, `git reset`, `git commit -a`, `--amend`.

REPORT (final answer; also to /tmp/claude-0/-shared-xv6iris-2/2c386d62-e2e4-4d9f-b3c5-841bab14908e/scratchpad/init-diag-report.md): the commit hash; every new definition and lemma statement verbatim; the pure facts you had to establish about `pro_alts`/`pro_round`/`wr_ban` (and any you could NOT establish -- with the exact goal); how each die diagnostic is written today (file:line) and what it would need; the gate lines. Plain concurrent-separation-logic and xv6 terms, every term of art defined on first use.
# ucode_echo.txt -- the pc set of iris/UCodeEcho.v, for tools/gen_ucode.py.
#
# This file is the pc set; tools/ucode_manifest.json is the row that binds it
# to iris/UCodeEcho.v, and `make gen-ucode` rewrites that file from it.
#
# It began as the hand-written EXAMPLE the generator was written against --
# every [udec_*] and [ui_echo_*] statement the two produced was
# character-for-character the same -- and the generator has owned the output
# since.

note echo's five reachable functions.  main and start diverge; strlen and
note the two syscall stubs return.

func main
func start
func strlen
func exit
func write

# one level down from main's diverging [jal ra,<exit>] at 0x78
omit 0x338 exit's [c.jr ra] -- the ecall at 0x334 never returns

skipdefault library or syscall stub that echo never calls

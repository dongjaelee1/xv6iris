BRIEF v9 (coordinator, 2026-09-16). THIS LAUNCH = M6a, THEN M3b CORE, each a green committed milestone reported separately. Landed since v8: IO-LEAF M5(3) (49cd3b5d1: sh's gets reads the era's line; `ush_rest` deleted, `sh_pay_rest` takes `ush_rest_l`; `ush_bnd`/`ush_posb`/the section parameter `Pm` with three laws at `Pm := UShLine.ush_mid γ`; the lease payload reassembles only at a line boundary; at the lend site `UkShFork.v:319` sh holds `⌜ush_bnd np0⌝ ∨ T`, `upos γp np0`, `ukn_pay N (-1)` -- and NOT the turn), TRAP-ROWS-4 A + B1a (wait's reaping arm at numbers; `upid`; `urun_ids`). In flight: TRAP-ROWS-5 (`-tlw`: init's pid = 1; the fork child's `⌜pidc <> 1⌝`; the wait leaf's `_pid` twin handing `⌜γ' ∈ Sc⌝ ∨ ⌜p = 1⌝`) -- sh's WAIT REDEMPTION waits for it and is the LAST step. READ THE HANDOVER io-leaf-handover.md FIRST (the M5b and M5(3) agents' sections: the `Rd` slot, `ush_mid`, `ush_posb`, why M3b core needs M6's `Rd` first; the rebase trap: `git apply --3way` STAGES its result, save patches with `git diff HEAD -- iris/`; the API gotchas and the two traps).

THE PRINCIPLE FOR BOTH MILESTONES (M4a(3)'s "pair, not a turn", now the rule): the U-tier programs name NOTHING of the era. Every credential crosses as an OPAQUE resource plus a PERSISTENT CONVERSION into the program's own obligation, both built ONCE at the top (`UInitBoot.echo_Hinit_boot`, where the era is named) from `EchoLinks.echo_links`.

M6a SCOPE -- THE WRITE CREDENTIAL RIDES THE LEASE: strengthen `Rd` (the lease slot `ucons_pay cn γ T Rd`'s payload, chosen once in `echo_Hinit_boot`) so `Rd n` carries, beside the era pin and the `dl_cnt` half, the write-side bundle at a ROUND START -- `∃ ps cs P, ⌜round_start ps cs n P⌝ ∗ turn v P ∗ ps_lb v ps ∗ cs_lb v cs ∗ E_lb v n` (the round-start conjunct EXISTENTIAL: the stage is not a function of the count alone; the prologue rounds `ps` are free) -- per the M5b handover this is one edit at two ends: `UInitBanner`/`UInitBoot` supply it at 0 (round 0's turn at stage 0 goes INTO `Rd 0`; the banner payment `kinit_banner0` is then DERIVED from `Rd 0` by a persistent conversion `□ (∀ n, Rd n -∗ <the round's banner payment at n> ∗ …)` built at the top, so init's proof pays its banner from the lease at EVERY round -- round 0 and the restarts alike -- and `Rt` (the prompt pair) comes out of the same conversion), and `UShLine`/`UShKernel` on sh's side: `ush_mid`/`ush_posb`'s left arm carries what `Rd` carries, so sh holds at every line boundary an opaque credential `C n` (`Rd n`'s write half) with, from the top, the persistent conversions sh needs: (i) into the prompt obligation (`sh_prompt_pay`'s mould, now for EVERY prompt, not just round 0's -- M4a(3)'s `ush_prompt_in ∨ True` gets its left arm on every back edge), (ii) into the child's lend (M3b), (iii) back from the child's payload at the alternative's end (M3b). Report the trusted diff (`Rd`'s definition at the top -- `echo_Hinit_boot`'s proof, not a statement; `init_boot_pay` if its shape moves; `kinit_banner0`/`kinit_round0`'s conversion; `Hsh_owed` untouched) and which of init's restart-loop arms now pay through the link (M6b = init's die arms and the round-k banner walk if any remain on the flagged deposit -- state exactly what remains).

M3b CORE SCOPE (after M6a) -- IN OPAQUE TERMS: at `UkShFork.v:319` sh lends the child `Rc := C np0` (the opaque line-boundary credential from `ush_posb`'s left arm; `emp` on the taint arm) and chooses `Q := fun _ => C' ∨ T` with `C'` the opaque credential at the alternative's end MINUS the prompt (three arms `∃ a` INSIDE `C'`'s top-level definition, not at sh's level); the top builds `□ (C np0 -∗ <UEchoOut.echo_uexec_slot_at's entry lend>)` and `□ (<echq's payload at the end> -∗ C')` so the exec of echo (`PinnedExec.Pay := C np0`, `UShEcho.v:1288-1312`'s kernel constructor replaced by `echo_uexec_slot_at` with its `echo_out_argv` theorem and the fd-1 row from `ush_fd2p`'s twin) and echo's exit meet sh's opaque shapes; the exec-failure refund (`UkShEcho.v:561`) kept and folded into `C'`'s alternative-1 arm (the child prints "exec echo failed\n" through `UkShDiag`'s chain forms -- M4b(2) pays it; for now the arm's diagnostic stays on the flagged deposit and the arm is stated); the null-store death's arm (alternative 2: the child records no choice). The kill wand `iRight`. Then, with TRAP-ROWS-5 landed, sh's wait: `wp_uk_ecall_wait_null_live`'s `_pid` twin + `upid` + the fork-side `⌜pidc <> 1⌝` identify the reaped child, `gen_pay_timeless` redeems `child_tok` against `exit_tok`, sh gets `C' ∨ T` and converts it (at the top) into the credential for its next prompt at the alternative's "$ ". If TRAP-ROWS-5 is not on main when you reach the wait, stop green there. FILES: UInitBanner.v, UInitBoot.v, UShLine.v, UShKernel.v, UInitSh.v, UShOut.v, UkSh.v, UkShFork.v, UkShRun.v, UkShEcho.v, UkShMain.v, UShEcho.v, UShEchoOut.v, UEchoOut.v (the constructor's premises only), UkInit.v/UkInitMain.v/UInitKernel.v ONLY if `Rd`'s conversion forces a one-line change (say so). Deny: UkShDiag.v/UkShCd.v (M4b(2) later), any kernel Spec*/Proof*, UexecRet/UkRunSys/UkStore/UkFork (TRAP-ROWS-5), EchoOut.v, App.v, AppEcho.v, UInitBootAdequacy.v (its statement must not change).

You are the agent for IO-LEAF (xv6iris, Rocq/Iris WP proofs of a RISC-V xv6 kernel with an "echo hello world" application theorem on top): the four user programs -- init, sh, sh's forked child, echo -- pay their console writes with the application's OUTPUT CLAIM and read the console through the application's READ LINK; the per-boot write credential ("the turn": `EchoOut.eturn`) travels init -> sh -> child -> echo and back; sh's free write law `UkSh.sh_deps` is replaced by the sound link law; the top theorem's `Hsh_owed` loses the `sh_deps` and read-leaf conjuncts. YOUR CHECKOUT IS /shared/xv6iris-2 (the main checkout) (absolute paths; never touch the other checkouts; `git fetch origin` only, never push; never touch main or claude-notes/). Start with `git fetch origin && git checkout -B lane/io-leaf origin/main`.

READ FIRST, IN FULL: scratchpad io-leaf-survey.md (THE SURVEY: §0 the three link statements verbatim, §1 the site table, §2 the turn's route, §3 the 49 `sh_deps` sites, §4 the read side fits, §5 the order M1-M6, §6 the warts W1-W8). Then claude-notes/projects/app-echo.md (the lane list; the LANDED notes for ECHO-OUT part 4/addendum/5, CONS-IO B/E/F, WRITE-LEAF, SELF-KILL step 5, TRAP-ROWS, FORK-REFUND, PROLOGUE-ALTS; "E5 DESIGN OF RECORD" R8), scratchpad e5-design-page.md §2, and the code the survey cites (iris/EchoOut.v links; UkInit*/UkInitMain; UkSh/UShLine/UkShRun/UkShFork/UkShEcho/UkShMain/UkShDiag; UkEcho; UkFork; PinnedExec; UkWriteLeaf/UkRunSys; UInitBoot/UInitSh/UInitBootAdequacy).

=== THE DECISIONS (coordinator, 2026-09-16; settled -- do not re-open) ===
D1 THE LINK LAW REPLACES `sh_deps`, IN `sh_deps`' OWN MOULD (survey W3/W4). The U-tier program files import `EchoOut` for its PREDICATES (`era_pin`, `turn`, `cs_lb`, `E_lb`, `dl_cnt`, `read_ret`, `eturn`) and take `` `{!echoOutG Σ} ``; they do NOT take the four section equations `Hout/Hin/Htag/Hwin`. The three link lemmas (`echo_write_link`, `echo_write_link_blk`, `echo_read_link`) are packaged ONCE in EchoOut as one PERSISTENT law `echo_links : iProp Σ := □ (∀ ..., <echo_write_link's statement as a wand>) ∗ □ (...blk...) ∗ □ (...read...)`, proved under the four equations (`Lemma echo_links_holds : ⊢ echo_links`), and the programs take `echo_links -∗` exactly where they took `sh_deps -∗` -- so `UkSh.sh_deps` is REDEFINED as `echo_links` (or deleted and every one of the 49 premise sites renamed; prefer whichever leaves lemma_diff smallest and say which), the two spend sites (`UkSh.v:942`, `UkShDiag.v:704`) change, and `Hsh_owed`'s `⊢ UkSh.sh_deps` conjunct is DISCHARGED at the top by `echo_links_holds` under the equations the record already fixes (where `echo_Hinit_boot` gets its equations). Init's own `udepw_law 16` (`UkInit.init_deps`, the die arms, the putc/printf/vprintf tower) goes the same way. `Tn` (`UInitKernel.init_boot_pay`'s third conjunct, `iClear`ed at `UkInitMain.v:2426`) becomes `eturn (S gen_id)`'s five conjuncts, concretely.
D2 THE CHILD'S DEATH AT A LINEAR PAYLOAD IS T3's PAIR (survey W1). After TRAP-ROWS T3, the fault arm at a killing cause is the ADDITIVE conjunction `ukill_cred_at gn sc ∧ uexec_kill_arm_F X sc W f`, produced from ONE copy of the program's resources by `iSplit`: the left (the deposit `kill_owed gn = ∃ Q, my_pay gn Q ∗ Q (-1)`) from the child's turn, the right (the resume) by the engine's Löb hypothesis. `UkSh.wp_ksh_memset_null`'s Coq-level `(⊢ ukn_pay N (-1))` premise (free only at `ukn_triv`) is therefore RESTATED as a resource premise at the child's payload; the same at `UkShRun.v:2504/2542/2844`, `UkShDiag.v:7669` and the three `ushp_pay_free` section hypotheses -- each becomes "the child holds its payload at -1" where the child actually does (the turn at the alternative's end ∨ T). If T3 as landed does NOT let the left side be paid from a linear resource, STOP and report -- do not add a workaround.
D3 THE PAYLOADS ARE STATUS-INDEPENDENT (`ukn_const`), ALWAYS `<turn bundle at the alternative's end stage> ∨ T`. sh's child: `Q := fun _ => (turn v P_end ∗ cs_lb v cs' ∗ E_lb v n ∨ T)` at the stage after `line_alts !!! a` for the alternative `a` the child took (0 echo ran, 1 exec failed, 2 died at the null store; 3 is sh's own `fork1` panic and is sh's, not the child's). echo's exit payload = the same at alternative 0. The fork-child wand `□ (riscv_kill_cred -∗ Q (-1))` is `iIntros "#HT"; iRight`. sh's own payload to init keeps `ucons_pay` and gains the turn conjunct at the alt-3 boundary, in the same `∨ T` mould; sh's own exit(0) after a -1 read is REFUTED by T2 (fd 0 is open), so `ush_at` need NOT assert `ukn_pay N (-1)` at every loop head any more (survey W6): unbundle `ush_at` into the lease, the cursor `upos γp n`, the era pin and the `dl_cnt` half (`era_pin k v ∗ dl_cnt v (1/2) n` beside `upos γp n`), and the turn at the cursor's stage; the payload is presented only where sh actually exits or forks.
D4 THE FAILURE REFUNDS ARE THE TURN. On fork failure T5 returns `Rc` (the turn) to init/sh, which then prints its diagnostic through the link at the prologue/line alternative PROLOGUE-ALTS/`line_alts` admit (survey W8). On exec failure `PinnedExec`'s refund (`pf_refund`, dropped today at `UkShEcho.v:558-562`) is kept; the child prints "exec echo failed\n" through the link at `line_alts !!! 1` and exits with the payload at that alternative's end.
D5 THE READ LEAF (survey §4; part 5's third owed conjunct): `UShLine.ush_read_sup` pays `cons_read_pay` with `EchoOut.echo_read_link` from `era_pin ∗ dl_cnt v (1/2) n` on the lease; `rf_in := read_ret`'s Φ; `ein_read_byte` gives `ush_line_is` per byte at sh's `cap = 1`; R2's `∃ q, n = 17 q` at the line boundary comes from `read_ret`'s `E_index`/`E_byte` and the loop invariant `n = 17 q + i`. `cons_read_pay_triv`, `in_licence_triv` and the flat `in_licence` premises go. This closes the third conjunct of `Hsh_owed`.
D6 THE ORDER = survey §5: M1 init's banner alone (the design risk in one build; `wp_kinit_write_chain`, the putc/printf/vprintf tower re-threaded with the turn: the loop invariant is "P bytes written, the next literal byte is `proc_upto cs0 (S n0) !! P`"); M2 echo; M3 the turn's transport (sh off `wp_uk_ecall_fork_any`/`uch_any` onto `wp_uk_ecall_fork`/`wp_uk_ecall_wait_null` at a named set; `ukn_triv` -> `ukn_const` + the payload equation); M4 sh's prompt and diagnostics (the `%s` token is pinned to "echo" by the parser's proof that feeds the pinned exec -- survey W7; state the lemma that pins it); M5 the read leaf (D5); M6 `echo_Hinit_boot` + `Hsh_owed` down to `⊢ sh_pay_rest sh_Rsh` alone. Each milestone lands green and committed on its own; report after M1 with the trusted diff (what `Hsh_owed`/`echo_Hinit_boot`/`init_boot_pay` look like) before M2.

D7 CORRECTIONS FROM THE OTHER LANES' PHASE-1 REPORTS (2026-09-16). (i) Init WRITES AGAIN after the banner: the next banner after an exec failure (the child printed "init: exec sh failed\n" and exited; init reaps and its loop re-enters) and its own "init: fork failed\n"; so the turn must come BACK to init through the child's exit payload before init's loop re-enters, with PROLOGUE-ALTS' `ps_lb` bound travelling with it; init's round-k banner bytes use `echo_write_link` at `ps0 = replicate k 1` via `pro_of_replicate_banner` (a lemma, not vm_compute); byte 19 of a round is written by init (`a = 2`), the child (`a = 1`) or sh (`a = 0`) through `echo_write_link_pro`. After sh's `fork1` panic ("fork\n", `line_alts !!! 3`) the transcript re-enters a fresh prologue block (PROLOGUE-ALTS' rounds). (ii) T1's short-write reason is an EXISTENTIAL over the chunk: `write_cons_short P ua k n := ∃ d, k <= d < n ∧ ~ uva_rmapped P (ua + d)` (consolewrite copies 32-byte chunks); the program refutes it from its own mapped buffer bytes. (iii) T2/T4 arrive as PURE rows on `usertrap_post` (`ut_live_out`): for a read at an open readable console fd with count >= 0, `r <> -1`; for a null-status-pointer wait, `r = -1 -> the caller's children column is empty`; the U tier reads them at `wp_uk_ecall_read_recv` and the wait leaf. (iv) T3's pair is free at the fault producers (`iSplit`); the child's null-store leaf pays the left side from `kill_owed gn` at its payload.

D8 THE FABLE REVIEW'S CORRECTIONS (scratchpad tasks-review-io-leaf.md, 2026-09-16; RULED, they override D1-D7 where they differ; read the review in full). (i) D1 as reviewed: `echo_links` in a sibling section of EchoOut under `riscvGS` alone, `echo_links_holds` inside the equation section (its §1 Coq statement); `read_ret` moves out of the equation section; the U tier inherits `γ : echo_gn` and `Timeless T` (UkSh has `Persistent` only); `sh_deps (PS := uprogSG_free)` at the two application sites is restated once `sh_deps` no longer depends on `PS`. `Tn` is ALREADY `EchoOut.eturn γ (S gen_id)` at the top (UInitBoot.v:726-742); only `wp_kinit_start`/`init_boot_pay` take it opaquely. (ii) D2: the engine restatement `Kc := Qp (-1)` (UkStore ×3, UkRunMem:415; `wp_uk_store_denied` taking the deposit as a RESOURCE instead of `(⊢ Qp (-1))`) is TRAP-ROWS M2's, delivered with T3 -- IO-LEAF consumes it. (iii) D3 stage indices: every `line_alts` entry ends in "$ ", written by SH after wait, so a child's payload is at the alternative's end MINUS 2; the null-store death records NO choice (sh's '$' is the block-first byte at `a = 2`); sh's child `Q` is a three-arm `∃ a`; sh's prompt site needs the three link shapes (`_pro`, W, W'); init's `Q` is an EXISTENTIAL over round-start stages (paid by the exec-failed init-child at one stage and by sh after "fork\n" at another); `gen_pay_timeless`/`uinit_redeem` go through; `Q (-1)` can no longer serve as sh's entry lend -- `UShKernel.sh_slot_of_kexec` (:645-650) and the constructors take the RAW bundle. (iv) D4/D7: two pure lemmas are PROLOGUE-ALTS' (a banner lemma at a round start after sh's "fork\n"; `echo_write_link_pro`'s `¬ pro_done` premise stated on the writer's own `ps0`, reconciled from the turn inside the step) -- if they are missing when you start, state them and stop. (v) D5/D6: M1 = ROUND 0 ONLY with the turn dropped after the banner (init's loop and the restarts move to M6, since rounds k>0 need the turn back through the child's payload); the printf tower is threaded with an abstract per-byte family `Ch : nat -> iProp` (the review's §4 statement) so EchoOut stays out of the tower, and `wp_kinit_printf` already is the no-'%' printf; a TAINT ROUTE for the write link is needed (`echo_write_link_taint : T -∗ (T -∗ Φ) -∗ out_link Uart0 k b Φ`, or the equivalent at the tainted claim) -- add it to EchoOut beside `echo_links` and prove it (the claim's taint arm is `T`). (vi) The `ukn_triv -> ukn_const` ripple is 23 mentions, but the real cost is a LINEAR payload through the 14 signatures step 5 touched plus the three `Hypothesis ushp_pay_free` (a section hypothesis cannot become a resource: restate them as resource premises); confine `uch_any -> named set` to the fork-wait window and DELETE the `_any` wrappers; `sh_exec_sup_echo` (a `□ ∀ N'` at the trivial payload fixed at sh's entry) must quantify the per-line stage inside the `□`, and echo's constructor/section become stage-parameterised.

M1 SCOPE (this launch; stop and report when it is green and committed): (a) a NEW FILE iris/EchoLinks.v (added to _CoqProject after EchoOut) holding D1's persistent law `echo_links` -- the conjunction of `echo_write_link`, `echo_write_link_blk`, `echo_write_link_taint` and `echo_read_link` as closed wands under `□` -- and `echo_links_holds : ⊢ echo_links` proved in a section that assumes EchoOut's four equations (the review's §1 statement); a NEW file so that PROLOGUE-ALTS-2, which is editing EchoOut.v right now, is not disturbed -- do NOT edit EchoOut.v; when `echo_write_link_pro` lands you (or M2's agent) add its conjunct. (b) `Tn` made concrete: `UInitKernel.init_boot_pay`'s third conjunct and `UkInitMain.wp_kinit_start`'s premise are `EchoOut.eturn γ (S gen_id)`'s conjuncts (already what `UInitBoot.v:726-742` supplies), no longer `iClear`ed. (c) Init's 18 banner bytes of ROUND 0 through `UkInit.wp_kinit_write_chain` (0 callers today) and the putc/printf/vprintf tower threaded with the review's abstract per-byte family `Ch : nat -> iProp` (§4), the loop invariant "P bytes written, the next literal byte is `proc_upto ps0 cs0 (S n0) !! P`" at `ps0 = []`, `cs0 = []`, `n0 = 0`, `pro_pin [] [] 0` (trivial), the byte facts by `vm_compute` on `u_prologue` (round 0 only; the arbitrary-round banner lemma is PROLOGUE-ALTS-2's); after the 18th byte the turn `turn v 18 ∗ ps_lb ∗ cs_lb ∗ E_lb` is DROPPED (affine) in M1 -- lending it to sh is M3's. The taint arm of each link (`∨ T`) continues the tower through `echo_write_link_taint`. Init's own `udepw_law 16` for the banner goes; `init_deps`' 16 stays supplied for the die arms (untouched in M1). (d) The short-write arm at row 16 (T1) refuted from init's own permission map (init's buffer is in its rodata/text: state the lemma). FILES M1 may edit: EchoLinks.v (new), _CoqProject, UkInit.v, UkInitPutc.v, UkInitPrintf.v, UkInitVprintf.v, UkInitMain.v, UInitKernel.v, UInitBoot.v (and UInitSh.v/UInitBootAdequacy.v only if `init_boot_pay`'s change forces it). Do NOT edit UkSh*.v, UShLine.v, UkEcho.v, UkFork.v, UkRunSys.v, UkWriteLeaf.v, UexecRet.v, UkStore.v, UkRunMem.v, EchoOut.v, EchoDisc.v, EchoOutPure.v, App.v, AppEcho.v. If (c) turns out to need a change in one of those, STOP at a green committed state and report why.

M2 SCOPE (DONE 2026-09-16 -- kept for reference; stop and report when it is green and committed): echo's proof pays its four console writes (`argv[i]` twice, " ", "\n" -- `UkEcho.v` `wp_kecho_write` at :1384/:1546/:1744 via `wp_kecho_write_chain` :1065, 0 callers) with the era's write link at a STAGE PARAMETER. Shape: echo's section/constructor become stage-parameterised (the review's §5: `sh_exec_sup_echo` in `UShEcho.v`/`UEchoKernel.v` is a `□ ∀ N'` at the trivial payload fixed at sh's entry; it must quantify the per-line stage inside the `□`). Echo holds, on entry, the turn bundle `turn v P ∗ ps_lb v ps0 ∗ cs_lb v cs0 ∗ E_lb v n0` at the stage after the '\n' echo of a completed line whose alternative `a = 0` was CHOSEN by echo's first byte 'h' (`echo_write_link_blk` at that first byte, returning `cs_lb v (cs0 ++ [0])`; then `echo_write_link` for the remaining bytes of "hello world\n" -- 11 bytes: byte facts by `proc_upto`'s block law at `line_alts !!! 0`); the taint arm continues through `echo_link_taint`. Echo's EXIT PAYLOAD (D3, corrected by the review): status-independent `ukn_const`, `Q := fun _ => (turn v P_end ∗ ps_lb ∗ cs_lb v (cs0 ++ [0]) ∗ E_lb v n0) ∨ T` at `P_end` = the stage after "hello world\n" (the "$ " of the alternative is sh's, after wait). The fork-child wand `□ (riscv_kill_cred -∗ Q (-1))` is `iRight`. Use `echo_links` (the projections) for the links and `uwrite_no_short` to read the post; the string bytes come from echo's argv on its stack/heap -- state the lemma that pins argv[1..2] to "hello"/"world" (the exec's pinned image + sh's line fact `ush_line_is` give it; if it is not available in echo's own hypotheses yet, take it as a premise of echo's constructor and say so -- M3 supplies it from sh's parse). Echo's `udepw_law 16` (`UkEcho.v:969/1214/1465/1643/1794/2357`, `UEchoKernel.v:446`, `UShEcho.v` 4 sites) goes for the console rows; state which rows (fd 1 not the console) still spend it, as M1 did. FILES M2 may edit: UkEcho.v, UShEcho.v, UEchoKernel.v, and a new small file for echo's link-side lemma on UInitBanner's mould if the U tier must stay below the file system (it must: UkEcho names nothing of the application). Do NOT edit UkInit*/UkInitMain/UInitBoot/UInitKernel/UInitBanner (landed M1), UkSh*.v, UShLine.v, UkFork.v, UkRunSys.v, UkWriteLeaf.v, UexecRet.v, UkStore.v, EchoOut.v, EchoLinks.v (unless a projection is missing -- then add it and say so), App.v, AppEcho.v. If echo's constructor's stage parameter forces a change in `UkShEcho.v` (sh's side), make the MINIMAL change that keeps it compiling at the trivial stage and report it -- M3 owns that file.

M3 SCOPE (M3a, M3b(1), M3c DONE 2026-09-16; M3b core's payload waits for M5 -- kept for reference; land as green committed milestones M3a/M3b/M3c, each reported): THE SHELL'S TRANSPORT OF THE CREDENTIAL, sh's side only. M3a -- sh's fork/wait onto the real leaves: sh's `fork1` chain (`UkShRun.wp_kshr_fork`/`_fork1`/`_fork1_final`, `UkShFork.wp_kshf_fork*`) moves off `UkFork.wp_uk_ecall_fork_any` onto `wp_uk_ecall_fork` with a real `Rc` and `Q`, and sh's `wait` (`UkShRun.v:~817`) off `wp_uk_ecall_wait_any` onto `wp_uk_ecall_wait_null` at a NAMED `uch (ukn_ch N) Sc` (confine `uch_any -> named` to the fork-wait window; DELETE the `_any` wrappers `wp_uk_ecall_fork_any`/`wp_uk_ecall_wait_any` in UkFork.v/UkRunSys.v once nothing uses them -- those two lemmas only); sh keeps `child_tok` and redeems the child's `exit_tok` via `gen_pay_timeless`. Every `⌜ukn_triv N'⌝` premise below (`UkShRun.v:993/1102/1512/1803`, `UkShEcho`, `UkShMain.v:102`) becomes `⌜ukn_pay N' = Q⌝` with `ukn_const` (the mould `ucons_pay_const`). M3b -- the payloads and the lend (D3 as corrected by the review): sh's child's `Q := fun _ => ∃ a, <turn at the end of line_alts !!! a MINUS the prompt> ∨ T` (three arms: a = 0 echo ran, `UEchoOut.echq`'s shape; a = 1 exec failed -- the child prints "exec echo failed\n" through the link at `line_alts !!! 1` from PinnedExec's REFUND, kept instead of dropped at `UkShEcho.v:558-562`; a = 2 the null-store death -- the child records NO choice, sh's '$' is the block-first byte at a = 2); `Rc` = the turn bundle at the line boundary after the '\n' echo (`turn v P ∗ ps_lb v ps0 ∗ cs_lb v cs0 ∗ E_lb v n0` at `n0 = 17 * S (length cs0)`), lent at fork, refunded by T5 on fork failure (sh's `fork1` panic then prints "fork\n" through the link at `line_alts !!! 3`, which OPENS a prologue round: PROLOGUE-ALTS' `echo_write_link_pro` is the next byte's link, but that is init's -- sh just exits with the turn in its own payload). The child's exec of echo: `PinnedExec`'s `Pay := Rc` (the bundle), transported into `UEchoOut.echo_uexec_slot_at`'s entry lend; M3 supplies echo's two named premises `echo_out_argv args` (argc = 3, argv[1..2] the line's tokens -- from sh's parse; state the lemma that pins them from `ush_line_is`) and the fd-1 console row of the child's table (inherited from init's pinned table through the exec channel: `take NSTD (uvis_fd W) !! 1 = Some (FdOpen rb true (FdDevice CONSOLE))`). sh's own payload to init keeps `ucons_pay` and gains the turn conjunct in the same `∨ T` mould (init's `Q` is an EXISTENTIAL over round-start stages -- state it; `gen_pay_timeless`/`uinit_redeem` go through since the turn is Timeless). The entry constructors (`UShKernel.sh_slot_of_kexec` :645-650) take the RAW bundle, not `Q (-1)`. M3c -- the child's death at the null store (`UkSh.wp_ksh_memset_null`, `UkShParse*`'s `ushp_pay_free`, `UkShRun.v:2504/2542/2844`, `UkShDiag.v:7669`): each Coq-level `(⊢ ukn_pay N (-1))` becomes a RESOURCE premise at the child's payload (a section hypothesis cannot become a resource -- restate them as premises), paid from the child's linear turn on the left of T3's pair. M3c REQUIRES TRAP-ROWS M2 part 2's engine statements (`UkStore.wp_uk_store_denied` at `Kc := Qp (-1)` taking the deposit as a resource); check origin/main for them when you reach M3c; if they are not landed, STOP after M3b green and committed and report. `ush_at`'s unbundling (D3: the lease, the cursor, the era pin + `dl_cnt` half, the turn at the cursor's stage; the payload presented only where sh exits or forks) is M3b's where the fork/exit sites need it and M5's for the read side -- do the minimum M3b needs and say what M5 inherits. FILES M3 may edit: UkShRun.v, UkShFork.v, UkShEcho.v, UkShMain.v, UkShDiag.v, UkSh.v, UShKernel.v, UInitSh.v, UShEcho.v, UkFork.v and UkRunSys.v (ONLY to delete the two `_any` wrappers), UkShParse*.v (M3c), PinnedExec.v (only if the refund's binder must be restated -- say so), a new small file above UkWriteLeaf on UEchoOut's mould for sh's link-side lemmas. Do NOT edit UkInit*/UkInitMain/UInitBoot/UInitKernel/UInitBanner/UInitFd/UInitCons (landed M1), UkEcho.v/UEchoOut.v/UEchoKernel.v (TXT-ROW is editing them), UexecRet.v/UkStore.v/UkLoad.v/SpecUsertrap.v/UserChildren.v/ProofKwait.v (TRAP-ROWS), EchoOut.v, EchoLinks.v (unless a projection is missing -- add it and say so), App.v, AppEcho.v, UInitBootAdequacy.v (its statement must not change in M3; `Hsh_owed` untouched).

M5 SCOPE (this launch; land as green committed milestones, each reported): THE SHELL'S CONSOLE READ THROUGH THE ERA'S READ LINK. (1) sh's lease carries the era pin and the delivered-count half: `UkSh.ush_at` (the lease + cursor `upos γp n`) gains `era_pin k v ∗ dl_cnt v (1/2) n` (D3's unbundling as far as the read side needs; the turn's stage is M3b core's), minted from `EchoOut.eturn`'s `dl_cnt v (1/2) 0` -- which travels the SAME route as the prompt credential (init's banner payment -> `Rt` -> the exec channel -> `sh_slot_of_kexec`): extend the pair `UShKernel.sh_prompt_pay`/`sh_prompt_at` (or a sibling `sh_read_pay`) so that init hands sh, beside the prompt obligation, the read-side bundle `∃ v, era_pin (S gen_id) v ∗ dl_cnt v (1/2) 0` in opaque form with its persistent conversion, exactly as M4a(3) did for the prompt (init names no era: the conversion is built in UShOut from `echo_links`' read projection `echo_links_rd`); `UInitBanner.kinit_banner0_pay_holds` instantiates it. (2) `UShLine.ush_read_sup` REBUILT on the console arm: `rf_in := read_ret`'s Φ, `cons_read_pay k Rin` paid by `EchoLinks.echo_links_rd` at `era_pin k v ∗ dl_cnt v (1/2) n` against `upos γp n` (`ush_rd_ret`'s `⌜cur = n⌝`), returning `read_ret T k v n ws` (its non-taint arm: `dl_cnt v (1/2) (n + length ws)`, `⌜read_ok pops dl ws⌝`, `⌜length dl = n⌝`, `⌜(dl ++ ws) prefix_of echoed pops⌝`, `E_index`/`E_byte` of `seg_of (echoed pops)`, and for `ws <> []` the bounds `cs_lb v cs0 ∗ ps_lb v ps0 ∗ E_lb v (n + length ws)`); `cons_read_pay_triv`'s use at the console arm and the flat `in_licence` go; the SHUT arm's `ush_read_sup_closed` stays. (3) sh's `gets` at `cap = 1`: `ein_read_byte` places each byte in the line by sh's own count (`x.2 = echo_line !!! (n mod 17)`), giving `UkSh.ush_line_is f k len` per byte -- the loop invariant `n = 17 q + i` -- and R2's line-boundary fact `∃ q, n = 17 q` at the `'\n'` (`i = 16`); `ush_read_ans`'s minus-one arm: the kill case is GONE at the user level (T2: `uexec_live_ok`'s read clause at an open readable console fd with count >= 0 gives `r <> -1`), so the -1 arm is refuted from `ush_fd0`/`ush_fd2p`-style console rows; `ush_swallow_taint` keeps `d = 0`. (4) `Hsh_owed`'s THIRD conjunct closed: the owed entailment `∀ c γp N l, ukn_pay N = ucons_pay fsc_cons γp (echo_taint c) -> ⊢ ush_read_recv_leaf N γp (echo_taint c) fsc_cons l` becomes a THEOREM (the leaf's statement may change with `ush_at`'s new conjuncts -- restate the leaf, prove it, and delete the conjunct from `Hsh_owed`; report `Hsh_owed` OLD/NEW verbatim: it drops from three conjuncts to two). (5) What M3b core receives: after reading the line, sh holds `E_lb v 17` (and `cs_lb`/`ps_lb`) beside `turn v 20` -- the bundle at the line boundary that `UkShFork.wp_kshf_fork:313`'s `Rc` needs; state it in the report as the exact resource sh holds at the fork site. FILES M5 may edit: UShLine.v, UkSh.v, UShKernel.v, UInitSh.v, UShOut.v, UInitBanner.v, UInitBoot.v, UInitBootAdequacy.v (ONLY to delete the third conjunct of `Hsh_owed`; the theorem's other hypotheses and its conclusion must not change), UConsLine.v, UkShLoop.v, UkShFork.v/UkShRun.v/UkShMain.v as the lease's new conjuncts force, EchoLinks.v only if a projection is missing (say so). Do NOT edit UkInit*/UkInitMain/UInitKernel (unless `Rt`'s instantiation forces a one-line change -- say so), UkEcho/UEchoOut/UEchoKernel, any kernel Spec*/Proof*, UexecRet, UkRunSys, UkStore, EchoOut.v, App.v, AppEcho.v.

M4b(1) SCOPE (a PARALLEL launch in /shared/xv6iris-2-sup on `lane/io-leaf-diag`, 2026-09-16; structural only, no semantic change): THE SHELL'S DIAGNOSTIC TOWER CARRIES THE PER-CALL OBLIGATION. Today every diagnostic the shell or its child prints (`UkShDiag.v`: `wp_kshd_putc` :~499 spends `sh_deps` at :~704; the printf/`%s` tower above it; 19 `sh_deps` premise sites in UkShDiag, plus the pass-throughs in UkShRun (`Hypothesis ush_diag_leaf` :~1207 and the `ush_diag_res` sites :~1168-1176), UkShFork, UkShMain, UkShCd, UkShEcho) is paid by the free law `sh_deps` (= `udepw_law 16`). Thread `UkSh.ksh_w` (landed in M4a(1): the per-call write obligation with the DESCRIPTOR as a parameter -- the diagnostics go to fd 2) through that tower exactly as M1 threaded `kinit_w1` through init's printf cone and M2 threaded `kecho_w` through echo's: each diagnostic site takes an abstract per-call family (a `Ci`/`Co` pair per `write`), the `%s` sites take the bytes of the runtime string as `ustr`/`utext` facts they already hold, and the tower's OLD statements are recovered verbatim as the instance at `ksh_w_of_law` (so no consumer moves and `lemma_diff` shows no CHANGED): keep `wp_kshd_putc`, `wp_kshr_fork1_final` etc. as corollaries of the new `_chain` forms. Do NOT pay any diagnostic with the era's link yet (that needs the turn at each site's stage -- M3b/M6); do NOT touch `Hsh_owed`; do NOT edit UkSh.v, UShLine.v, UShKernel.v, UInitSh.v, UkShLoop.v, UkInit*/UkInitMain/UInit*/UserConsole (IO-LEAF M5b is in them in the main checkout), nor any kernel file, UexecRet, UkRunSys, UkFork (TRAP-ROWS-4). FILES: UkShDiag.v, UkShRun.v, UkShFork.v, UkShMain.v, UkShCd.v, UkShEcho.v, UkShParse*.v if a pass-through is there, a new small file for the diagnostics' link-side lemmas ONLY if it is stateable now without the turn (otherwise leave it to M4b(2)). Report: the family's definition verbatim, which of the 19 sites became `_chain` forms, the `%s` token facts each site needs (and whether the parser's proof already pins them), the gate, and what M4b(2) (the payment) receives. Builds `dg1`, `dg2`, ... in -sup; the standard gate and rebase rules.

RULES: build only with `./gcp-rocq/vmbuild.sh xv6iris-2 <log>` from /shared/xv6iris-2 (names `io1`, `io2`, ...; one build at a time; a run-on-gcp SYNC IS A BUILD -- never run the rocq-warm loop while a vmbuild is in flight; never start a build before the previous log has its EXIT line; vmbuild drops every dirty .vo; the VM tree /mnt/rocq/trees/_shared_xv6iris-2 -- check `git -C /mnt/rocq/trees/_shared_xv6iris-2 log -1` via run-on-gcp; it was last built at e58f284a2's iris state, so your first build rebuilds PROLOGUE-ALTS' cone too). Never pattern-kill. Never push. Commit by explicit path on `lane/io-leaf`, author `Nickolai Zeldovich <nickolai.zeldovich@gmail.com>` (the checkout's config; never `-c user.email`). Gate before reporting: zero Error, EXIT=0, `make -f CoqMakefile -n` prints no `ROCQ compile`, `make audit-only 2>&1 | grep -v '^make\|^cd ' | md5sum` = 57f7327206c4b276d05035342fea8ecf, `python3 tools/lemma_diff.py --ref origin/main` with every GONE/CHANGED justified, NO Admitted, no new assumption, `./gcp-rocq/run-on-gcp --check-dumps` clean; at completion `git fetch origin`, rebase onto the then-current origin/main (save-patch by path / `git checkout HEAD -- <paths>` / `git merge --ff-only origin/main` / `git apply --3way`; never stash or reset), ONE confirming build, then report: the trusted diff verbatim (`init_boot_pay`, `wp_kinit_start`, `echo_links`, `echo_Hinit_boot` if touched, `Hsh_owed` if touched -- it should not be in M1), the file list and cone, the gate lines, the as-landed note (<= 20 lines), and what M2..M6 receive. Keep io-leaf-handover.md current in the scratchpad; if context runs short, stop at a green committed milestone and say where a successor picks up.

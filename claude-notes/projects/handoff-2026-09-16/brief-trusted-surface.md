You are the agent for lane TRUSTED-SURFACE of the xv6iris project (Rocq 9.0.1 / Iris weakest-precondition proofs of a RISC-V xv6 kernel with an "echo hello world" application theorem on top). READ-ONLY: you edit NO file under any checkout and start NO build; your deliverable is a document. Work in /shared/xv6iris-2-tlw (absolute paths; `cd /shared/xv6iris-2-tlw && git fetch origin && git checkout -B lane/trusted-surface origin/main`, origin/main = 7da574e81 code; never touch /shared/xv6iris-2, -disc, -sup, -notes; do not run ./gcp-rocq/run-on-gcp at all -- read sources only).

READ FIRST: claude-notes/README.md; claude-notes/durable-notes.md; claude-notes/projects/checkpoint-2026-09-14-step3.md; the previous trusted-surface documents claude-notes/projects/handoff-2026-09-16/trusted-surface-2026-09-14.md, trusted-surface-2026-09-16b.md, trusted-surface-2026-09-16.md (the mould: sections, tone, what counts as trusted); claude-notes/projects/handoff-2026-09-16/endgame-audit.md; claude-notes/projects/app-echo.md notes dated 2026-09-14 ("IO-LEAF STEP 3 LANDED", "PROLOGUE-ALTS-3 LANDED", "INIT-DIAG LANDED", "M6b LANDED", "IO-LEAF STEP 4 LANDED", "DIE-DW CORRECTED") and "E5 -- THE CONSOLE I/O CLAIM: DESIGN OF RECORD"; tools/proof_coverage.py and tools/lemma_diff.py (what the audit counts); the build's audit target (`make audit-only` in the Makefile: what it prints -- the thirteen ambient assumptions). THE CODE, the trusted statements themselves: iris/UInitBootAdequacy.v (the theorem `echo_adequacy_modulo_phi` and `Hsh_owed`, verbatim); iris/SystemAdequacy.v / the adequacy chain it instantiates (grep `Hinit_boot`, `boot_fixedGS`, `riscv_rx_tag = app_tag`); iris/EchoDisc.v (the trace predicate: `expected_rel`, `sess`, `pro_alts`, `pro_of`, `pro_cont`, `pro_done`, `line_alts`, `alt_seq`, `alt_blk`, `disc`, `disc_pt`, `good_out`, `u_banner`, `u_prompt`, `echo_line`, the `demo_*` anti-vacuity witnesses); iris/UInitKernel.v (`init_boot_pay` -- trusted, NEW shape with `Wp` and `kinit_diag_law`); iris/UInitSh.v (`sh_pay_rest` -- trusted; `ush_rest_l`'s body in iris/UkSh.v that it quantifies, incl. `ush_bstate`); iris/SystemAssumptions.v (deliberately outside _CoqProject); the six descoped `# Foo.v` rows of iris/_CoqProject and what each descopes; the FsImg/ElfUser/UCodeInit/ShSyms/InitSyms pinned-binary facts (what is computed from the real disk image vs assumed).

THE TASK: write the refreshed trusted-surface document as of main 7da574e81 (step 4 landed), on the mould of the previous ones, for the owner: (1) the theorem statement verbatim and in plain words (what "every thread stays reducible" and "the UART wire in every power cycle is a prefix of an admitted transcript" mean, what "modulo phi" is); (2) EVERY hypothesis it still takes, verbatim, with a plain reading and its status (`Hsh_owed`'s two conjuncts: what each says, who discharges it in the plan -- RESIDUALS is deleting the first, R3 the second); (3) the trace predicate as it stands after PROLOGUE-ALTS-3, in plain words, including the known widening (per round any word over {1,3} then 0 or 2) and the owner's ruling that it is accepted for now, with the five machine transcripts and their `demo_*` witnesses; (4) the thirteen ambient assumptions of `make audit-only` (list them by name with one line each on why each is an assumption and not a proof); (5) what is COMPUTED from the real artifacts (the disk image, the ELF binaries, the instruction bytes) versus ASSUMED (SystemAssumptions.v, the descoped rows), and the trust in the tools (the Sail RISC-V model, the ISA slice, the boot ROM/`boot_fixedGS`); (6) the changes since trusted-surface-2026-09-16b.md, each with OLD/NEW verbatim (`init_boot_pay` twice, `sh_pay_rest`, `pro_alts`/`pro_of`/`pro_cont`/`pro_done`, `ush_rest_l`'s inner state, the deleted `sh_prompt_*`/`kinit_round0`/`die_dw`), and for each whether the theorem got STRONGER, WEAKER or EQUIVALENT and why; (7) the remaining placeholders ON the trusted path if any (grep `∨ True`, `Admitted`, `admit`, `Axiom`, `Parameter`, `Hypothesis` inside the files the theorem's cone names -- report every hit under iris/Uk*.v, USh*.v, UInit*.v, UEcho*.v, Echo*.v and say which are section hypotheses discharged at the top and which are not); (8) a one-page "what you are trusting" summary in plain English for someone who has not read the proofs.

REPORT (final answer; also to /tmp/claude-0/-shared-xv6iris-2/2c386d62-e2e4-4d9f-b3c5-841bab14908e/scratchpad/trusted-surface-2026-09-14b.md): the document itself, markdown, plain concurrent-separation-logic and xv6 terms, every term of art defined on first use; verbatim Coq in fenced blocks; file:line for every claim. Do not speculate where you can read.

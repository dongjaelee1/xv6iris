You are the agent for lane R3-SURVEY of the xv6iris project (Rocq 9.0.1 / Iris weakest-precondition proofs of a RISC-V xv6 kernel with an "echo hello world" application theorem on top). READ-ONLY: you edit NO file under any checkout and start NO build; your deliverable is a design report. Work in /shared/xv6iris-2-sup (absolute paths; `cd /shared/xv6iris-2-sup && git fetch origin && git checkout -B lane/r3-survey origin/main`, origin/main = 7da574e81 code; never touch /shared/xv6iris-2, -disc, -tlw, -notes). You may use the VM read-only for `About`/`Print`/`Search` through rocq-warm ONLY if the tree's .vo are current -- they are NOT after a checkout switch and a sync IS a build that could collide with lane RESIDUALS (which builds in -disc, a different tree; yours is `_shared_xv6iris-2-sup`) -- prefer reading the sources; if you must query Rocq, one `./gcp-rocq/run-on-gcp` sync of YOUR tree is allowed, then `--no-sync` queries.

READ FIRST, IN FULL: claude-notes/README.md; claude-notes/durable-notes.md; claude-notes/projects/checkpoint-2026-09-14-step3.md; claude-notes/projects/app-echo.md notes "SH-LINE R3 SURVEYED", "IO-LEAF STEP 4 LANDED", "M6b LANDED", "IO-LEAF STEP 3 LANDED", "PROLOGUE-ALTS-3 LANDED", and every note mentioning `ush_gen_slot`, `ush_rest_l`, `sh_pay_rest`, `sh_rest_holds`, `uxsup`, "composer", "E4" (grep); claude-notes/projects/handoff-2026-09-16/r3-report.md (the PRE-step-4 survey: its (A)/(B)/(C) findings and its ruled route (1)-(4)); step4-sh-report.md (whole); step3-sh-report.md §7 (D1: why `ush_rest_l` gained the two lease laws). THE CODE: iris/UInitBootAdequacy.v (`Hsh_owed`; the theorem statement); iris/UInitSh.v (`sh_pay_rest`, `sh_pay`, `sh_pay_of_parts`, `sh_Rsh`, `sh_pay_state_holds`, `init_sh_slot`); iris/UShRest.v if it exists (grep `sh_rest_holds` tree-wide); iris/UkSh.v (`ush_gen_slot`, `ush_rest_l`, `ush_rest_line`, `ush_loop_head`, `wp_ksh_loop`, `ush_bstate`, `ush_pstate`); iris/UkShFork.v (`ushf_rest_of_body`, `wp_kshm_body`, `wp_kshf_fork`, `ushf_child_law`, `ushf_kill_law`, `ushf_wq`, `ushf_fans`, the affine/taint arm's generic child `UkShMain.wp_kshm_child_alloc`); iris/UkShEcho.v (`wp_kshm_child_echo`, `sh_exec_sup_echo`, `ush_echo_round_carry`); iris/UShEchoPay.v (NEW in step 4: `ushf_child_law_holds_at`); iris/UkShLoop.v; iris/UkRun.v (`uxsup`, `udep`, `udepw_of_uxsup`); iris/UexecExecMint.v (`uslot_mint_all`); iris/UInitBoot.v (`echo_Hinit_boot`: `Hsh_rest`, `Hsh_deps`, `Htsw`/`Hstw`, `init_deps_of_sup`, every `Hsh_*`); iris/AppEcho.v (`echo_sup_of_taint`, `echo_taint_of_sup`, `app_sup`).

THE QUESTION: after step 4, what EXACTLY closes `Hsh_owed`'s second conjunct `(⊢ UInitSh.sh_pay_rest UInitSh.sh_Rsh)`, i.e. what is the top-level discharger of `ush_rest_l N γp T Wc Wb Pm (sh_Rsh …)` for EVERY record `N`, and what stands in its way. The pre-step-4 survey found three blockers: (A) `ushf_rest_of_body` (the only prover of `ush_rest_l`) spends the GENERIC exec supply `uxsup`, which at the echo era exists only under the taint (`app_sup ⊣⊢ echo_taint`), and its disciplined arm is the generic body, not the pinned composer with E4's dispatch; (B) `ush_gen_slot N T` is per-record and buildable only where the record's constant payload is paid by the taint, so it must move INSIDE `ush_rest_l`'s `□` (paid by `wp_ksh_loop`); (C) the cwd was `ucwd_any`. Step 4 changed the ground: the fork arm now runs the PAID echo child on the console arm (E4's dispatch through `ushf_child_law`), the cwd is pinned at ROOTINO in the loop state, and lane RESIDUALS (running now, in -disc) is killing every remaining affine arm and making the free law `sh_deps` taint-conditional (`□ (T -∗ sh_deps)` on the paid walk), so the affine/taint arm's generic child becomes a TAINT-only arm. Answer, with file:line evidence and verbatim statements: (1) `ushf_rest_of_body`'s current full premise list, and which premises the top cannot supply for an arbitrary `N` (`uxsup`? `ush_gen_slot N T`? `ukn_const N`? `Hpsok_free`? the laws?); (2) whether `uxsup` is needed anywhere off the taint arm after RESIDUALS' (C)/(D), and if only on the taint arm, whether `□ (T -∗ uxsup)` is derivable at the top (`UkRun.uxsup` from `app_sup` from `echo_taint`: find the constructor or say none exists and what one would need); (3) the exact statement change to `ush_rest_l` that puts `ush_gen_slot` inside its box (the trusted obligation's TEXT changes -- draft OLD/NEW verbatim; `sh_pay_rest`'s text stays) and where `wp_ksh_loop` pays it (`uslot_mint_all` needs the constant payload `fun _ => R` and `□ (riscv_kill_cred -∗ R)`: what is `R` for sh -- `ukn_pay N (-1)` at the pair family -- and is the kill wand available at the top: `Hktaint`); (4) whether a NEW composer file is still needed (the old plan's (3)) or whether `ushf_rest_of_body` at the paid child law + `□ (T -∗ uxsup)` + the boxed generic slot IS the composer, and if a new file is needed, its exact statement; (5) the exact shape of `UShRest.sh_rest_holds` (or whatever closes the conjunct) and the exact edit at `echo_Hinit_boot` / `UInitBootAdequacy` that deletes the conjunct (OLD/NEW of `Hsh_owed`); (6) an ordered edit list (file, lemma, change) for a follow-on lane, with the collision risks against RESIDUALS' files, and a sober estimate of the design risk ("the one place a design surprise is still plausible" -- say whether it still is).

REPORT (final answer; also to /tmp/claude-0/-shared-xv6iris-2/2c386d62-e2e4-4d9f-b3c5-841bab14908e/scratchpad/r3-survey-2.md): the six answers with evidence; plain concurrent-separation-logic and xv6 terms, every term of art defined on first use (a reader who has not seen this project must be able to follow). Do not speculate where you can read; where a fact needs Rocq, say so and what query settles it.

You are the agent for lane R3 of the xv6iris project (Rocq 9.0.1 / Iris weakest-precondition proofs of a RISC-V xv6 kernel with an "echo hello world" application theorem on top). YOUR CHECKOUT IS /shared/xv6iris-2-sup (absolute paths everywhere; never touch /shared/xv6iris-2, -disc, -tlw, -notes; never push; never edit claude-notes/; iris/_CoqProject gets ONE bare file row for the file you add, right after the row of the highest file it requires). You are the only lane editing code.

SETUP: `cd /shared/xv6iris-2-sup && git fetch origin && git checkout -B lane/r3 origin/main` (origin/main = d1cc70d8b code + notes commits; the checkout is clean on a stale read-only branch, abandon it). Confirm `git log --oneline -3`. Your VM tree is /mnt/rocq/trees/_shared_xv6iris-2-sup; its first build rebuilds whatever EXEC-SEAM changed (~150 files) -- start it (`./gcp-rocq/vmbuild.sh xv6iris-2-sup r3-0`, detached, poll) BEFORE you edit, while you read.

READ FIRST, IN FULL: claude-notes/README.md; claude-notes/durable-notes.md (whole); claude-notes/projects/checkpoint-2026-09-14-step3.md; claude-notes/projects/checkpoint-2026-09-16.md §6 (build/gate/commit rules); claude-notes/projects/handoff-2026-09-16/r3-survey-2.md (WHOLE -- §5 is the design of record: the new UShRest.sh_rest_holds and the edits that delete Hsh_owed's second conjunct; §6 is your ORDERED EDIT LIST; §8 the three Rocq queries to run first); /tmp/claude-0/-shared-xv6iris-2/63890815-951e-4376-a8bd-ec450556fa07/scratchpad/exec-seam-report.md (the EXEC-SEAM lane report) (what (B)/(C)/(D) changed under you: ush_pstate, ush_wcp, ushf_rest_of_body's premises, sh_deps' shape, Hsh_owed's current text); app-echo.md notes "EXEC-SEAM LANDED", "SH-LINE R3 RE-SURVEYED", "IO-LEAF STEP 4 LANDED", "RESIDUALS (A) LANDED"; step4-sh-report.md §2.5 and §3. THE CODE: iris/UkSh.v (`ush_rest_l`, `ush_gen_slot`, `ush_gen_run`, `wp_ksh_loop` ~7500, `wp_ksh_main`, `wp_ksh_console`), iris/UkShFork.v (`wp_kshf_fork`, `wp_kshm_body`, `ushf_rest_of_body`, the taint arm), iris/UkShEcho.v (`ush_line_toks_holds`), iris/UkShLoop.v (`ush_line_lexable`, `ushl_R`), iris/UShEchoPay.v (the mould for the era laws by `iPoseProof`: `ushf_child_law_holds_at`, `ushf_kill_law_holds`), iris/UShPanic.v (`ush_panic_law_holds`), iris/EchoLinksLine.v (`ewc_lcred`, `ewc_lcred_blk_line`, `ewc_lcred_timeless`), iris/UInitSh.v (`sh_pay_rest`, `sh_pay`, `sh_pay_of_parts`, `sh_Rsh`), iris/UShKernel.v (`sh_kexec_sz`), iris/UInitBoot.v (`echo_Hinit_boot`: `Hsh_rest`, `Hlks`, `Hturn`, `Hsh`, `Hsh_wbr`), iris/UInitBootAdequacy.v (`Hsh_owed` and its two uses).

THE TASK (plain terms). The top theorem still assumes the shell's REST-OF-LINE obligation (`UInitSh.sh_pay_rest sh_Rsh`: for every record, taint and credential family, the command loop body holds the line fact), because the body's only proof (`UkShFork.ushf_rest_of_body`) needs the ERA's laws and the generic slot, which no `∀ T Wc Wb Pm` statement can supply. RULED (R3 re-survey): DELETE `sh_pay_rest` and the conjunct, and DISCHARGE the obligation at the echo era's own families through ONE new glue lemma `UShRest.sh_rest_holds` (r3-survey-2.md §5, statement verbatim there), built at `echo_Hinit_boot` from the links, the deposit, the echo slot and an era pin. Follow §6's ordered edit list exactly: (1) UkSh: `ush_gen_slot` and the wb-assembler into `ush_rest_l`'s box, paid by `wp_ksh_loop`; (2) UkShFork: the fork's taint arm to the generic slot through `ush_gen_run`, `ushf_rest_of_body` takes the laws from the box; (3) `ush_line_lexable_holds`; (4) NEW iris/UShRest.v with `sh_rest_holds`; (5) UInitSh: delete `sh_pay_rest`, `sh_pay_of_parts` at the new middle premise; (6) UInitBoot's four edits; (7) UInitBootAdequacy: `Hsh_owed`'s second conjunct deleted -- if EXEC-SEAM (D) already deleted the first, delete the WHOLE binder, the `destruct` and both Coq premises of `echo_Hinit_boot`, and print the audit and explain every line (the hypothesis count changes). Run §8's three Rocq queries first and adapt to what they say. Where the survey and the tree disagree (EXEC-SEAM moved under it), the tree wins; make the smallest change consistent with §5 and REPORT it. TRUSTED: `Hsh_owed` OLD/NEW verbatim.

RULES: everything fully proved; no Admitted; no new `∨ True`; pure facts by `vm_compute`/`reflexivity` in small closed lemmas, never on goals with variables; `(PS := uprogSG_free)` pinned on every deposit-bearing term on both sides of every `iApply`; era laws introduced by `iPoseProof` as NAMED hypotheses, never `iIntros "#…"` on a bundle of wands (durable-notes); every file must `Require` what it names; name Timeless/Persistent instance binders explicitly; grep the whole tree for users before deleting; no prose in iris/_CoqProject.

BUILD PROCEDURE (never deviate): rocq-warm per file between builds (claude-notes/rocq-warm.md; a run-on-gcp sync deletes the .vo of every edited file -- rebuild edited dependencies first with `make -f CoqMakefile -j8 A.vo` in the VM tree). Full builds from /shared/xv6iris-2-sup: `./gcp-rocq/vmbuild.sh xv6iris-2-sup r3-1` (then r3-2, ...), DETACHED (`setsid nohup ... > /tmp/claude-0/-shared-xv6iris-2/63890815-951e-4376-a8bd-ec450556fa07/scratchpad/r3-1.out 2>&1 &`), then POLL the .out file for its `EXIT=` line with short sleeps FROM YOUR OWN TURN -- NEVER end your turn while a build is running; errors via `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/r3-1.log'`. One build at a time in your tree; never start one before the previous log has its EXIT= line; never pattern-kill on the VM.

GATE before you report: EXIT=0 and zero ^Error; VM `make -f CoqMakefile -n` prints no `ROCQ compile`; `cd /mnt/rocq/trees/_shared_xv6iris-2-sup && make audit-only 2>&1 | grep -v '^make\|^cd '` printed in full and every line explained against the previous thirteen (md5 57f7327206c4b276d05035342fea8ecf before (D)); `python3 tools/lemma_diff.py --ref origin/main` with every GONE/CHANGED justified (GONE must be `UInitSh.sh_pay_rest` plus nothing unexplained); no Admitted; `./gcp-rocq/run-on-gcp --check-dumps` clean. Commit by explicit path on lane/r3 (author = the checkout's config; never `-c user.email`; end the message with `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01LhojXpyqkCYPo6pkDjDckg`). Never `git stash`, `git add -A`, `git reset`, `git commit -a`, `--amend`. Green milestones as separate commits if natural ((1)-(3) the shell side; (4)-(7) the discharge), else one.

REPORT (final answer; also to /tmp/claude-0/-shared-xv6iris-2/63890815-951e-4376-a8bd-ec450556fa07/scratchpad/r3-report.md): commit hashes; `Hsh_owed` OLD/NEW verbatim and the theorem's binder list after; `UShRest.sh_rest_holds`'s statement verbatim; `ush_rest_l`'s NEW body; every deleted name and where its users went; the audit text and its explanation; the gate lines; anything the survey got wrong. Plain concurrent-separation-logic and xv6 terms, every term of art defined on first use. If you run out of context, stop at a green committed state and write where a successor picks up.

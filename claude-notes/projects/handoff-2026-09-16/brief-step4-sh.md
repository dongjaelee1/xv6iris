You are the agent for lane STEP4-SH of the xv6iris project (Rocq 9.0.1 / Iris weakest-precondition proofs of a RISC-V xv6 kernel with an "echo hello world" application theorem on top). YOUR CHECKOUT IS /shared/xv6iris-2-disc (absolute paths everywhere; never touch /shared/xv6iris-2, -sup, -tlw, -notes; never push; never edit claude-notes/; iris/_CoqProject gets a bare file row only if you add a file). A sibling lane M6B-INIT works concurrently in -sup on the INIT side (UkInit.v, UkInitMain.v, UInitDiag.v, UInitBanner.v, UInitKernel.v and the init half of UInitSh.v/UInitBoot.v). DO NOT edit UkInit.v, UkInitMain.v, UInitDiag.v, UInitBanner.v, UInitKernel.v, UInitFd.v, UserConsole.v at all. In UInitSh.v and UInitBoot.v edit ONLY what the shell's new Coq-level premises and instantiations force (the coordinator merges the two lanes' edits to those two files by hand -- keep yours localized and LIST every hunk in the report).

SETUP: `cd /shared/xv6iris-2-disc && git fetch origin && git checkout -B lane/step4-sh origin/main` (origin/main = b65275c81 or later; the checkout is clean on lane/step3-sh which is merged). Confirm `git log --oneline -3`. Your VM tree is /mnt/rocq/trees/_shared_xv6iris-2-disc.

READ FIRST, IN FULL: claude-notes/README.md; claude-notes/durable-notes.md (whole file); claude-notes/projects/checkpoint-2026-09-14-step3.md (the latest checkpoint: §1 rulings, §3b, §4); claude-notes/projects/checkpoint-2026-09-16.md §6 (build/gate/commit rules); claude-notes/projects/app-echo.md notes dated 2026-09-14: "IO-LEAF STEP 3 LANDED", "PROLOGUE-ALTS-3 LANDED", "INIT-DIAG LANDED", "DIE-DW CORRECTED", "SH-LINE-CRED LANDED", "IO-LEAF WRITE-CLOSED LANDED", "IO-LEAF M6a(3) REVIEWED", "SH-LINE R3 SURVEYED", and the older "IO-LEAF M6a(2) LANDED", "IO-LEAF M6a(1) LANDED"; claude-notes/projects/handoff-2026-09-16/io-leaf-handover.md ("THE REST OF THE LANE" and "STEP 3 -- THE DESIGN OF RECORD"); handoff-2026-09-16/step3-interface.md; handoff-2026-09-16/step3-sh-report.md (whole); handoff-2026-09-16/prologue-alts-3-report.md, -report-2.md, -report-3.md ("State for the coordinator" -- the exact lemmas you consume); handoff-2026-09-16/init-diag-report.md (§1 and "HOW EACH DIE DIAGNOSTIC IS WRITTEN TODAY" -- for the shapes the shell's own diagnostics will mirror). THE CODE: iris/UkSh.v (`Context (Wc)`/`(Wb)`/`(Pm)` block, `ush_wcp`, `ush_wcp_cons`, `ksh_w_of_wcp`, `ush_posb*`, the hypotheses, `ush_gets_done*`, `ush_pstate`, `ush_rest_l`, `ush_loop_head`, `wp_ksh_loop`, getcmd, main's exit); iris/UkShFork.v (whole: `ushf_pay`, `ushf_pstate_at`, `wp_kshf_fork` -- the fork arm at ~285-340 and the parent's re-entry at ~455-500, the child arm, `wp_kshf_fork_any`, `wp_kshm_body`, `ushf_rest_of_body`); iris/UkShDiag.v (`wp_kshr_fork1_final` ~8644, `wp_kshd_panic`/`_chain`, `ksh_w1_of_law`, `ush_diag_leaf_holds`, the diagnostics "exec %s failed" / "open %s failed" / "cannot cd"); iris/UkShRun.v (`wp_kshr_wait` ~821, `wp_kshr_wait0`, `wp_kshr_fork` ~975, `wp_kshr_fork1` ~1476); iris/UkRunSys.v (`wp_uk_ecall_wait_null_pid` ~2321, `wp_uk_ecall_wait_null`), iris/UexecRet.v (`uwait_ans`, `uwait_ans_pid`, `uwait_ans_pid_mine` ~1087); iris/ChildTok.v (`child_tok`, `gen_pay`, `my_pay`, `kill_shot`); iris/UkShEcho.v (`ush_pstate_at`, `wp_kshm_child_echo` ~781, `wp_kshm_child_echo_holds`, `ush_echo_round_carry`); iris/UEchoOut.v (`echo_uexec_slot_at` ~747 -- the echo child's PAID entry), iris/UShEcho.v (`sh_echo_slot`, `sh_exec_sup_of_echo_slot*` -- the GENERIC entry the child runs on today); iris/EchoLinks.v, iris/EchoLinksLine.v, iris/EchoLinksBan.v, iris/EchoLinksPro.v (every `ewc_*`, every `echo_*_step`, `ewc_read`, `ewc_panic_done`, `echo_prompt_dollar_ban`, `ewc_owed_read_taint`, `ewc_line_read_taint`, `ewc_ban_read_taint`, `ewc_ban_owed`, `ewc_ban_line`); iris/UShLine.v (`ush_mid`, `ush_rd_x`, `ush_posb_of_lend`, `ush_mid_wc_read`, `ush_read_recv_era`, `ush_read_recv_leaf_holds`); iris/UShOut.v, iris/UShPanic.v (`ksh_w_of_link_cred`, `ksh_w1_of_link_panic`, `ksh_w_of_link_lcred`, `sh_prompt_law_holds`, `sh_prompt_law_holds_line`); iris/UShKernel.v (`sh_uexec_slot`, `sh_slot_of_kexec`, `wp_ksh_start`, `sh_prompt_law`); iris/UInitSh.v (`init_exec_sup_of_sh_slot`, `sh_pay`, `sh_pay_rest`), iris/UInitBoot.v (`init_cons_sup_of_sh_slot`, `echo_Hinit_boot`'s assembly: every `Hsh_*` assert and the `Wc`/`Wb`/`Rdl` instantiations).

THE TASK: IO-LEAF STEP 4 = M3b core + the wait redemption + consuming PROLOGUE-ALTS-3's two lemmas; then M4b(2) (the shell's diagnostics) in the same checkout, as a separate green commit. Plain terms: today the shell's command loop holds the console lease's PIECES `Pm n` and a credential slot `UkSh.ush_wcp Wc Wb l n p` with three arms -- both-console (`Wc n p`), closed fd 2 (`Wb n`), and an AFFINE `∨ True`. The `∨ True` exists because the FORK arm of the loop (sh forks, the child execs echo, the parent waits) re-enters the loop head with no credential: the parent assembles its exit payload on the affine arm before fork1 (`UkShFork.v` ~298: `ush_posb_at` drops the slot), lends the child NOTHING (`Rc := emp`, the child's payload `Q := fun _ => True` at `wp_kshr_fork1_final`), and re-enters through `ush_posb_of`'s True arm (~482). Step 4 makes the fork LEND and the wait REDEEM:
(1) THE LEND. At the fork the shell holds `Pm n' ∗ ush_wcp l n' 0` with `n'` the boundary after the line ("echo hello world\n" read; `ush_gets_done_line` left the credential at `n'` through `ush_wc_read`). The child's write credential is that slot's both-console arm: `Rc := Wc n' 0` (the shape `echo_link_blk` wants at the child's first byte 'h'; the tight family from SH-LINE-CRED, `EchoLinksLine.ewc_lcred`/`ewc_line`, is what the child's block and the shell's next prompt are stated at -- decide, with the reports, whether the loop's `Wc` at the top becomes the tight family widened with the ban arm (the SH-LINE-CRED finding says it must), and say so). The child runs echo on the PAID entry `UEchoOut.echo_uexec_slot_at` (E4's dispatch `UkShEcho.wp_kshm_child_echo_holds` is the pinned composer's child arm; read how the generic `UShEcho.sh_exec_sup_of_echo_slot` is used today and route the paid one), and its exit payload family `Q` carries the credential AFTER the block (the shell's next prompt is paid from it: `UShPanic.ksh_w_of_link_lcred` / `EchoLinksLine.ewc_line`), or the taint `T` (a killed child: `□ (riscv_kill_cred -∗ Q (-1))`, the taint pays -- `UkShFork` cannot name the era, so this is an opaque law routed like `ush_prompt_law`).
(2) THE REDEEM. The parent's wait (`UkShRun.wp_kshr_wait`) moves to the `_pid` twin (`UkRunSys.wp_uk_ecall_wait_null_pid` with the shell's own pid row `UserChildren.upid (ukn_pid N) …` -- find where sh's pid row lives in its run/state; `wp_kshr_wait`'s `_pid` twin is owed in UkShRun) and `UexecRet.uwait_ans_pid_mine`: the reaped child is the one sh forked, its token `child_tok γ pidv Q` redeems `Q (-1)`-shaped payload (`ChildTok.gen_pay` -- see how init's wait redeems in UkInitMain.wp_kinit_main_loop ~1600, `gen_pay_timeless`, `gen_uniq_tok`), and the credential lands in `ush_wcp l n' 0`'s both-console arm at the loop's back edge (the ledger is unchanged across the fork on the parent's side). The `-1` arm of wait ("my own child set is empty") is refuted against the live child token (the DIE-DW CORRECTED recipe, mirrored for the shell). The taint arm of the payload goes to `ush_posb`'s taint arm.
(3) CONSUME PROLOGUE-ALTS-3: `ush_wcp_cons` at `k = 2` sends the closed arm to the CREDENTIAL arm through a new UkSh hypothesis `ush_wb_wc : ∀ n, ⊢ Wb n -∗ Wc n 0` (discharged at the top from `EchoLinks.ewc_ban_owed` / `EchoLinksBan.ewc_ban_line`); `ush_gets_done_line`'s closed arm (fd 2 closed, fd 0 the console, a line read at an unwritten prompt) is REFUTED through a new hypothesis of the shape `ush_wb_read : ∀ n, ⊢ Pm (n + 17) -∗ Wb n -∗ T` (or whatever exact shape `UShLine.ush_read_recv_era`'s window arm + `ewc_ban_read_taint` support -- the read's receipt carries `turn_lb`/`rd_stage` now; the T-form sends the walk to the taint arm, which is the honest outcome), discharged in UShLine.
(4) KILL the affine arms by name: `UkSh.ush_wcp`'s third arm; `UShLine.ush_rd_x`'s `(Wb n ∨ True)` = `UkInit.init_rd_cred` -- the init side's file is NOT yours: state what the shell side now guarantees (every shell exit hands back `Wb n`: the shut-fd-0 exit's closed arm, the fork panic's exit, the taint arm) and leave `init_rd_cred`'s deletion to the coordinator's merge, keeping your tree green against the current `UkInit`. Every `∨ True` that remains after your commit must be named in the report with the obligation that kills it.
(5) M4b(2), AFTER (1)-(4) are green and committed: the shell's diagnostics through the links. The fork PANIC ("fork\n" at fd 2, `wp_kshd_panic`) pays as line alternative 3 from `Wc n' 0` through `UShPanic.ksh_w1_of_link_panic`/`EchoLinksLine.echo_panic_step` and ends at `ewc_panic_done : ewc_panic v n 5 -∗ EchoLinks.ewc_ban T v n 0` = `Wb n'`, which assembles the exit payload (`ush_at_of_pm_wb`); "exec %s failed" in the child needs the token pinned to "echo" (the line is `EchoDisc.echo_line`, the parser's first token; E4's `UConsLine.ush_echo_tokens`) and pays as the line alternative the transcript admits for it (read `EchoDisc.line_alts`); "open %s failed" and "cannot cd" are REFUTED (the line has no redirection and is not `cd`: `UkShLoop.ush_line_lexable`, `UkShEcho.ush_line_toks_holds`), not paid. Then the free write law `sh_deps` is no longer spent in UkShDiag: say exactly which spenders of `sh_deps` remain (checkpoint §3b lists four families).

RULES: everything fully proved; no Admitted; no new `∨ True` (every affine arm you leave is one of the named three, or named in the report with its killer); pure facts about literals by `vm_compute`/`reflexivity` in small closed lemmas, never on goals with variables; every file must `Require` what it names (a transitive import can vanish under a sibling's edit -- durable-notes); name Timeless/Persistent instance binders explicitly (an anonymous second one is `H0` and clashes with the proofs' asserts); do not rename what you need not; grep the whole tree for a lemma's users before deleting it; do not add prose to iris/_CoqProject. Section-variable orders: check `About` for every UkSh definition whose binder list changes and put the lists in the report.

BUILD PROCEDURE (never deviate): edit loop with rocq-warm (claude-notes/rocq-warm.md): `./gcp-rocq/run-on-gcp -q bash -lc 'cd /mnt/rocq/trees/_shared_xv6iris-2-disc/iris && /mnt/rocq/rocq-warm/rocq-warm check UkSh.v'` (the sync deletes the .vo of every file you edited, so once you touch A.v a warm check of B.v that imports A fails with "Cannot find a physical path bound to logical path A"; rebuild A first: `./gcp-rocq/run-on-gcp -q bash -lc 'cd /mnt/rocq/trees/_shared_xv6iris-2-disc/iris && make -f CoqMakefile -j8 A.vo'`; a stale intermediate gives "makes inconsistent assumptions" -- make its .vo too). Full builds: from /shared/xv6iris-2-disc, `./gcp-rocq/vmbuild.sh xv6iris-2-disc s4-1` (then s4-2, ...), DETACHED (`setsid nohup ./gcp-rocq/vmbuild.sh xv6iris-2-disc s4-1 > /tmp/claude-0/-shared-xv6iris-2/2c386d62-e2e4-4d9f-b3c5-841bab14908e/scratchpad/s4-1.out 2>&1 &`), then POLL the .out file for its `EXIT=` line with short sleeps in a loop from your own turn -- a detached build does not wake you when it finishes, so never end your turn with a build running unless you have written the report; errors via `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/s4-1.log'`. One build at a time in your tree; never start one before the previous log has its EXIT= line; never pattern-kill on the VM (other agents build there); a run-on-gcp sync IS a build. UkSh.v (~7400 lines) and UkShDiag.v (~8600) take minutes to check; batch edits.

GATE before you report: EXIT=0 and zero ^Error; VM `make -f CoqMakefile -n` prints no `ROCQ compile`; `cd /mnt/rocq/trees/_shared_xv6iris-2-disc && make audit-only 2>&1 | grep -v '^make\|^cd ' | md5sum` = 57f7327206c4b276d05035342fea8ecf (the thirteen; if different, print the audit and explain every line); `python3 tools/lemma_diff.py --ref origin/main` with every GONE/CHANGED/NEWAXIOM justified (a new UkSh section Hypothesis shows as NEWAXIOM -- say where it is discharged); no Admitted; `./gcp-rocq/run-on-gcp --check-dumps` clean. TRUSTED STATEMENTS: `Hsh_owed` (UInitBootAdequacy.v), `UInitSh.sh_pay_rest`, `UInitKernel.init_boot_pay`, `EchoDisc`'s predicate -- report OLD/NEW verbatim for any you change (`sh_pay_rest` will gain binders if `ush_rest_l` does). Commit by explicit path on lane/step4-sh (author = the checkout's config; never `-c user.email`; end the message with `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01VzSVeaFhoeWNGQosZ6V4gt`). Never `git stash`, `git add -A`, `git reset`, `git commit -a`, `--amend`. Land green milestones as separate commits: (A) M3b core + wait redemption + the two PROLOGUE-ALTS-3 consumptions; (B) M4b(2).

REPORT (final answer; also to /tmp/claude-0/-shared-xv6iris-2/2c386d62-e2e4-4d9f-b3c5-841bab14908e/scratchpad/step4-sh-report.md): the commit hashes; every new/changed definition and lemma statement VERBATIM (the fork's `Rc`/`Q`, the redeem lemma, the new UkSh hypotheses and where each is discharged, `ush_wcp`, `wp_kshr_wait`'s twin, the diagnostics' laws); the `About` binder lists; every deleted name and where its users went; EVERY `∨ True` left in the shell's files, by name, with the obligation that kills it; the exact hunks you made in UInitSh.v and UInitBoot.v; which spenders of `sh_deps` remain; the gate lines; anything you had to decide that this brief did not settle. Plain concurrent-separation-logic and xv6 terms, every term of art defined on first use. If you run out of context, stop at a green committed state and write where a successor picks up.
